# Continuo 本地环境变量示例。复制为 .env.local(已 gitignore)后填值。
#
# 安全 S4(codex 安全审计):marketplace 插件 reviews 需要 GitHub token 才能拉取
# (GitHub GraphQL 必须 auth)。**token 只在 main 进程使用**,通过运行时环境变量
# GITHUB_TOKEN 读取,绝不内联进 renderer 产物。
#
# 切勿再用 VITE_ 前缀承载密钥 —— Vite 会把 VITE_* 内联进 renderer bundle,任何
# renderer 内插件 / 拿到打包应用的人都能提取(凭据泄漏)。
#
# 只读 public_repo 权限的 PAT 即可;不配置则 reviews 优雅降级(只用已缓存数据)。
# 打包发布时**不要**在带真实 token 的机器上构建 —— 现已不内联,但仍勿把密钥带进产物。
GITHUB_TOKEN=
