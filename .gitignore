node_modules
out
out-pack
dist
dist-electron
.DS_Store
*.log
.vite
.cache
.env
.env.local
*.tsbuildinfo
coverage
test-results
playwright-report
# examples/ 整体 ignored(大参考项目不入库),demo plugin 例外。
# 注意:git 不会进入完全 ignore 的目录,所以必须先 examples/* 再 !。
examples/*
!examples/sample-plugin/
!examples/mcp-demo-plugin/
!examples/claude-code-adapter-minimal/
!examples/codex-cli-adapter-minimal/
# Dev-loop / il-loop 状态;议题 B.1 默认不入 git
.claude/
# Codex CLI 本地 cache + config (可能含 user/api secrets);永远不入 git
# (topic 46 v4-E commit-pathspec 守护即时 commit, .gitignore 永久保护)
.codex/
# Plan 02 临时执行报告;commit 前 user 可删
EXECUTION_CONTEXT.md
