import { describe, it } from 'vitest';

describe('window sequence service API', () => {
  it.todo('T8: exposes service helpers to allocate, read, ensure, and persist window entries');

  it('T8: keeps allocation and persistence APIs scoped to main-process ownership', () => {
    throw new Error('not implemented (BDD red)');
  });
});
