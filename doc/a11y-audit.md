# 可访问性(a11y / 键盘 + 屏幕阅读器)审计(第十四 session 续,codex 协作,/goal 一次一个)

> 方向:键盘用户无法操作 / 屏幕阅读器用户无法理解的真实 a11y 缺陷。规则:本仓 macOS-only tested,只修 jsdom/unit 可验证或纯逻辑客观项(断言 DOM role/aria-*/tabindex/键盘 handler),不报需真实 AT/视觉/对比度主观判断的项。优先用 @/design 已封装 a11y 的组件;native/绕过 design 的自写控件是缺口高发区;design 共享层(src/design)有 design-layer 约束默认不改,缺口在调用点则在调用点修。

## 逐项修复日志

- **A1 修(P2,交互元素无可访问名)**:`CommandPalette.tsx:173` 命令面板搜索 `<Input>` 只有 placeholder,无 aria-label/aria-labelledby/关联 label → 屏幕阅读器聚焦时只读「编辑框」无名(placeholder 不是可靠可访问名:部分 AT 不读、输入后消失)。→ 补 `aria-label={t('command_palette.placeholder')}`(复用同一 placeholder key,design Input 转发 `...rest` 故 aria-label 透传到 `<input>`)。+TDD(open 后 `.wm-modal-content input` 的 aria-label 非空且与 placeholder 一致,locale-无关)。typecheck + 3451 测试 + lint 绿。
- **A2 修(P2,A1 同族,Quick Open 搜索框无可访问名)**:`QuickOpenModal.tsx:174` 搜索 Input 只有 placeholder 无 aria-label。与 A1 不同:placeholder 含 `{shortcut}` 快捷键参数(「搜索文件名/路径片段(⌘⇧P 切到命令面板)…」),不适合作可访问名。→ 新增**专用简洁** `quick_open.input_aria`(三语言「搜索文件」,无快捷键,仿已有 quick_open.list_aria 模式)作 aria-label。+TDD(open 后 input aria-label 非空且 != placeholder,locale-无关)。typecheck + 3452 测试 + lint 绿。**沉淀:placeholder 含动态/快捷键参数时不能直接复用作 aria-label,应另起简洁的专用 *_aria key。**
- **A3 修(P2,A1 同族,Marketplace 搜索框无可访问名)**:`MarketplaceTab.tsx:297` 搜索 Input 只有 placeholder 无 aria-label。placeholder 无参数(「搜索插件名/描述/标签…」)→ 按 established pattern(无参数复用、含参数才专用 key,见 A1/A2)复用 `marketplace.search_placeholder` 作 aria-label。+TDD(搜索 input aria-label 非空且 == placeholder)。typecheck + 3453 测试 + lint 绿。**搜索框无可访问名族:CommandPalette(A1)+ QuickOpen(A2)+ Marketplace(A3)。**
- **A4 修(P2,role=switch 有状态无名)**:`SettingItemRow.tsx:138` Boolean 设置项自写 `ToggleSwitch`(button role="switch" + aria-checked)无可访问名、未关联左侧设置标题 → 屏幕阅读器只读「switch checked/unchecked」不知切换哪个设置。→ 给 ToggleSwitch 加必填 `ariaLabel` prop,调用点传已算好的 `title`(spec.titleKey ? t : spec.title),组件设 `aria-label={ariaLabel}`。+TDD(boolean switch 的 aria-label == 设置标题)。typecheck + 3454 测试 + lint 绿。**新子类:有 role+aria 状态但缺可访问名 —— role=switch/checkbox/radio 等自写控件须 aria-label 或 aria-labelledby 关联其标签。**
- **A5 修(P2,搜索/输入框无名族「修一族必修兄弟」批量收口)**:`SettingsPanel.tsx:100` 设置搜索 Input 无 aria-label(codex 报告)。**按全仓 grep 所有带 placeholder 的 `<Input>` 一次性闭合该族**(A1-A3 已修 3 个,TerminalSearchBar 本已有 aria-label):新修 5 处 —— SettingsPanel 搜索(复用 placeholder)/ KeybindingsTabContent 搜索(复用)/ CreateInput 新建文件夹·文件名(placeholder 本身是标签,同值复用)/ PluginsTabContent git-url input(placeholder 是 URL 示例非标签 → 复用 section 标题 `plugins_tab.section.install_from_git` 作 aria-label)/ MarketplaceTab git-url input(复用 `marketplace.git_url_section_title`)。+TDD(SettingsPanel 搜索 input aria-label 非空且 == placeholder)。typecheck + 3455 测试 + lint 绿。**所有带 placeholder 的输入框均已有 aria-label,无名族闭合。沉淀:placeholder=格式示例(URL)时不能复用作 aria-label,用 section 标题等真标签;placeholder=真标签(搜索框/新建名)时同值复用即可。**
- **A6 修(P2,菜单触发按钮缺 aria-haspopup/aria-expanded)**:`ExplorerHeader.tsx:156`「更多操作」⋯ IconButton 切换 `role="menu"` 弹层,但按钮本身无 aria-haspopup/aria-expanded → 屏幕阅读器只读「更多操作 button」不知会弹菜单/是否已展开。→ 补 `aria-haspopup="menu"` + `aria-expanded={menuOpen}`(IconButton 转发 ...rest 故透传)。+TDD(aria-haspopup=menu + aria-expanded 随点击 false↔true 切换)。typecheck + 3456 测试 + lint 绿。**新子类:弹层触发控件须 aria-haspopup + aria-expanded 暴露「可弹出 + 当前展开态」给 AT。**
- **A7 修(P2,A6 同族 Dock 菜单触发按钮 + 全仓闭合)**:`HeaderActions.tsx:127` Dock 面板头部「更多操作」⋯ IconButton 切换 `role="menu"` 弹层但无 aria-haspopup/aria-expanded(同 A6)。→ 补 `aria-haspopup="menu"` + `aria-expanded={open}`。**按「修一族必修兄弟」全仓 grep `role="menu"`**:ExplorerHeader(A6)+ HeaderActions(A7)两个按钮触发弹出已修;EmptyWorkspace 的 `role="menu"` 是常驻 recent 列表(非按钮触发弹出)不属此模式不改。**菜单触发弹出族闭合**。+TDD(aria-haspopup=menu + aria-expanded 随点击切换)。typecheck + 3457 测试 + lint 绿。
- **A8 修(P2,toggle 缺 aria-pressed)**:`MarketplaceTab.tsx:485` tag filter `TagButton` 是 toggle,选中态只通过 className 改视觉,无 aria-pressed → 屏幕阅读器听到 tag 名但不知是否已选中。→ 补 `aria-pressed={active}`。**按「修一族必修兄弟」全仓 grep toggle 按钮**:TerminalSearchBar regex/case/word(已有 aria-pressed)、design NavRailButton(已有)→ 仅 TagButton 缺,已补。+TDD(tag button aria-pressed 随点击 false→true)。typecheck + 3457 测试 + lint 绿。**新子类:toggle 按钮(选中态只改 className)须 aria-pressed 暴露给 AT。**
- **A9 修(P2,展开/折叠按钮缺 aria-expanded)**:`MarketplaceTab.tsx:707` Reviews 展开/折叠按钮只用 title + ▴/▾ 表状态,无 aria-expanded → 屏幕阅读器不知 review 列表是否展开。→ 补 `aria-expanded={expanded}`。全仓 grep 展开/折叠按钮:此为唯一独立展开按钮(FolderTree 树项用 headless-tree 管 aria-expanded)。+TDD(byPid 设 rating 渲染展开按钮,aria-expanded 随点击 false→true)。typecheck + 3458 测试 + lint 绿。**新子类:展开/折叠(disclosure)按钮须 aria-expanded(≠ A6/A7 的 aria-haspopup=menu;disclosure 只需 aria-expanded,无 haspopup)。**
- **A10 修(P1,设计层 Modal aria-hidden 包裹可聚焦内容)**:`Modal.tsx:116` overlay 设 `aria-hidden="true"` 却包裹 `wm-modal-content`(含所有 children=可聚焦控件)→ **屏幕阅读器忽略整个弹窗内容**(CommandPalette/QuickOpen/AgentAuthPrompt/PermissionPrompt 等全部 Modal),焦点 trap 进 aria-hidden 子树(WAI 反模式)。这是 a11y 方向唯一 P1。**位于设计层 src/design**(CLAUDE.md「默认不改」),但:(1) 该文件已有 Continuo-local 微调先例(modalKeyStack);(2) 这是 a11y 正确性 bug 非 i18n(design-layer-no-keys 铁律只禁 i18n 渗透,不禁 aria);(3) 通用修复应推回 Nous。→ **移除 overlay 的 aria-hidden** + content 加 `role="dialog"` + `aria-modal="true"`(置于 {...rest} 前,允许调用方覆盖,配合已有 focus trap 成完整无障碍 dialog;ConfirmDialog 经 rest 传的 aria-labelledby 保留)。加 Continuo-local 注释标记。+TDD(overlay 无 aria-hidden / content role=dialog+aria-modal / 可聚焦内容不在 aria-hidden 子树 / rest 的 aria-labelledby 保留;design-layer-no-keys 仍绿)。typecheck + 3461 测试 + lint 绿。**沉淀:aria-hidden 绝不能包裹含可聚焦/可交互内容的子树(WAI «hiding focusable content» 反模式);Modal overlay 既当 backdrop 又当 content 容器时不能整体 aria-hidden。设计层 a11y 正确性 bug 可按 Continuo-local 约定修(区别于 i18n 铁律)。**
- **A11 修(P2,tab 导航无选中态)**:`SettingsPanel.tsx:123` 左侧设置 tab 导航按钮只用 className 表 active,无 aria-selected/aria-current → 屏幕阅读器不知当前所在设置页。→ nav 是独立可 tab 的 `<button>` 列表(非箭头键 roving 的 ARIA tab 控件),故用 `aria-current={active?'page':undefined}` 暴露选中态,而非 role=tab/aria-selected(避免强加不完整的 tab 键盘交互模型)。+TDD(active 项 aria-current=page、其余 null、切换跟随)。typecheck + 3462 测试 + lint 绿。**沉淀:导航式 tab(独立可 tab 按钮,点击切换)用 aria-current=page;真 ARIA tab widget(role=tablist/tab+aria-selected)须配箭头键 roving tabindex,不可只加 role 不加交互。**
- **A12 修(P2,不可见但可聚焦的占位按钮 + 同族)**:`SettingItemRow.tsx:101` reset「恢复默认」IconButton 未覆盖默认值时仅用 `pointer-events-none invisible` 隐藏(常驻占位防布局跳动),但**仍可 Tab 聚焦** → 键盘用户 Tab 到不可见且点击无效的按钮。→ 加 `disabled={!isOverridden}`(从 tab 顺序移除 + 不可点,布局占位仍保留)。**按「修一族必修兄弟」**:codex 提示的 `KeybindingsTabContent.tsx:218` reset 占位按钮完全同模式,一并加 disabled。+TDD(setting-item-row reset 按钮未覆盖 disabled、setValue 覆盖后启用)。typecheck + 3463 测试 + lint 绿。**新子类:`invisible`/`opacity-0` 视觉隐藏但保留布局的占位交互元素仍在 tab 顺序/AT 树中 → 须 disabled(交互元素)或 aria-hidden+tabIndex=-1;只 invisible 不够。**
- **A13 修(P2,A10 后续族:dialog 无可访问名 + 全仓闭合)**:A10 把 Modal content 设为 role="dialog" 后,无 aria-labelledby/aria-label 的弹窗被 AT 念成「无名 dialog」(进入弹窗听不到这是什么)。codex 报 `PermissionPrompt.tsx:50` + 提示同文件 fs-scope 弹窗。**按「修一族必修兄弟」全仓核查所有 `<Modal>` 调用点**:有可见标题的加 `aria-labelledby`(标题元素加稳定 id)—— PermissionPrompt 授权(permission-prompt-title)+ fs-scope(permission-fs-scope-title)、AgentAuthPrompt(agent-auth-title)、PermissionEditorModal(permission-editor-title)、KeybindingCaptureModal(keybinding-capture-title);无可见标题的加 `aria-label` —— CommandPalette(复用 placeholder key)、QuickOpen(复用 input_aria);ConfirmDialog 本已有 aria-labelledby。+TDD(PermissionPrompt dialog 有 aria-labelledby 且指向非空标题)。typecheck + 3464 测试 + lint 绿。**沉淀:给 Modal 加 role=dialog 后必须配可访问名(aria-labelledby 指向标题 / aria-label),否则 role=dialog 反而让 AT 念「无名对话框」;一次 a11y 改动(A10)会派生新需求(A13),须连带全仓补齐。**
- **A14 修(P2,resize separator 键盘不可达)**:`ExplorerSidebar.tsx:45` 侧栏 resize handle `div role="separator"` 只有 onMouseDown + aria-label,无 tabIndex/键盘处理/aria-value* → 键盘用户无法聚焦或调宽度,AT 不知当前宽度范围/值(WAI window-splitter 模式缺失)。→ 加 `tabIndex={0}` + `aria-valuemin/max/now`(min=200/max=500/now=width)+ onKeyDown(ArrowLeft/Right=±16、Home/End=min/max,经 clampWidth 钳制,setSidebarWidth 不自带 clamp)+ focus-visible 高光。+TDD(separator tabindex=0/aria-value*/Arrow±16/Home min/End max)。typecheck + 3465 测试 + lint 绿。**新子类:鼠标拖拽控件(splitter/slider/resize handle)须键盘可操作 —— tabIndex + Arrow/Home/End + aria-value*(window-splitter/slider 模式),只有 onMouseDown 等于键盘用户完全不可用。**
- **A15 修(P2,combobox 缺 aria-activedescendant)**:`CommandPalette.tsx:181` 输入框键盘控制下方 `role="listbox"`,option 有 aria-selected 但焦点始终留输入框、输入框无 aria-activedescendant 指向当前高亮 option → 屏幕阅读器按上下键不知高亮哪条。→ 标准 combobox 模式:input 加 `role="combobox"` + `aria-expanded` + `aria-controls`(指向 listbox id)+ `aria-activedescendant`(指向当前 option id);listbox 加稳定 id;每个 option 加 `id={...option-${idx}}`。**按「修一族必修兄弟」QuickOpen 同模式一并修**(quick-open-listbox / quick-open-option-${idx})。+TDD(input role=combobox + aria-activedescendant 指向真实存在的 option 且随 selectedIndex 移动)。typecheck + 3466 测试 + lint 绿。**新子类:input-controls-listbox(焦点留输入框、虚拟选中)是 combobox 模式 —— 仅 option aria-selected 不够,须 input aria-activedescendant 指向当前 option id(option 须有稳定 id)。**
- **A16 修(P2,A8 同族:排序按钮组无状态)**:`MarketplaceTab.tsx:746` Reviews 排序按钮组(Newest/Helpful)当前 sort 只用 className 表视觉,无 aria-pressed → 屏幕阅读器不知当前排序。→ 单选 toggle 组,加 `aria-pressed={sort === s}`(pressed 项=当前排序;同 A8,比 radiogroup/radio 更简单且足够)。+TDD(展开后恰 1 个 sort 按钮 aria-pressed=true、点另一个翻转)。typecheck + 3467 测试 + lint 绿。**toggle/单选按钮组族(A8 tag filter + A16 sort)统一用 aria-pressed 暴露选中态。**
- **A17 修(P2,A12 同族:pointer-events-none 视觉禁用但键盘可达)**:`SettingsPanel.tsx:112` 搜索模式下左侧 nav 整体 `pointer-events-none opacity-40`(只鼠标禁用),但内部 tab 按钮仍可 Tab 聚焦 + Enter/Space 触发 → 键盘用户能在搜索结果模式下切换被视觉禁用的隐藏 nav。→ 给 nav 按钮加 `disabled={inSearch}`(从 tab 顺序移除 + 阻断键盘激活,与视觉禁用一致)。+TDD(非搜索 nav 可用、输入搜索后全 disabled)。typecheck + 3468 测试 + lint 绿。**沉淀:`pointer-events-none` 只挡鼠标不挡键盘 —— 容器级视觉禁用必须对内部交互元素同时 disabled(或 tabIndex=-1 + aria-disabled),否则键盘用户绕过禁用态(A12 单元素 + A17 容器级,同根)。**
- **A18 修(P2,焦点管理:role=menu 打开焦点不移入)**:`ExplorerHeader.tsx`「更多操作」⋯ 菜单(+ dock `HeaderActions.tsx` 面板菜单同模式)打开后只把 `role="menu"` portal 挂 DOM,**焦点仍停在触发按钮**,键盘用户无法直接方向键/Enter 操作菜单项,也无 Escape 关闭+还原焦点。→ 抽共享 hook `src/lib/use-menu-keyboard.ts`(WAI-ARIA menu 键盘契约:挂载移焦入首个可用 menuitem / Escape 关闭+还原焦点到 trigger / ArrowDown·ArrowUp 循环漫游 + Home·End),两个菜单消费点统一接入(menuRef callback ref + menuNode 供 pointerdown contains)。+TDD(打开移焦入「展开全部」/ ArrowDown 到「切换文件夹…」/ Escape 关菜单且焦点回 ⋯ 按钮)。typecheck + 3469 测试 + lint 绿。**沉淀:portal 菜单的 initial-focus 不能用 `useEffect([open])` —— open 翻 true 那次 render 菜单尚未挂载(坐标 anchor 在 layoutEffect 后才算、菜单延一帧挂),effect 不会为后续 render 重跑;须用 callback ref,节点真正 attach 时才聚焦。菜单焦点管理是「修一族必修兄弟」典型:两处同模式菜单抽单一来源 hook。**
- **A19 修(P2,错误 role:常驻列表误标 role=menu)**:`EmptyWorkspace.tsx:37` 最近目录是常驻列表却用 `role="menu"` 容器 + MenuItem(`role="menuitem"`),非弹出菜单、无 roving focus/Arrow 模型 → 屏幕阅读器误读成应用菜单,且 menuitem 脱离 menu 父级本就是非法 ARIA。→ 改 `<ul aria-label>` + `<li>` + design `Button variant="ghost"`(role=button,左对齐全宽保持视觉),移除 menu/menuitem 语义。更新现有 2 个钉死 menu/menuitem 的测试断言正确 list 语义(无 role=menu/menuitem、ul 有 aria-label、行项是 li>button、点击 setRoot)。typecheck + 3469 测试 + lint 绿。**沉淀:role=menu/menuitem 仅用于「弹出菜单(配合移焦+Arrow roving,见 A18)」;常驻的动作列表用 ul/li + button(role=button)。MenuItem 设计组件强制 menuitem role,常驻列表场景改用 Button ghost 而非 MenuItem。**
- **A20 修(P2,焦点管理:Modal 初始焦点忽略子元素意图)**:`Modal.tsx:55` 初始焦点的 rAF 总聚焦第一个可聚焦元素,忽略调用方意图 → `ConfirmDialog` 确认按钮(排在取消之后)声明的 `autoFocus` 被 rAF 覆盖到「取消」按钮,危险操作的初始焦点落错。→ design 层 a11y 正确性修(Continuo-local,同 A10):Modal rAF 优先聚焦 `[data-autofocus]:not([disabled])`,找不到再回退第一个可聚焦;ConfirmDialog 确认按钮改用 `data-autofocus`(取代 React `autoFocus` —— 后者在 commit 期 imperatively 聚焦、不渲染 DOM 属性查不到,且会污染 prevFocus 还原)。+TDD(Modal 有/无 data-autofocus 两路;ConfirmDialog 打开焦点落确认按钮;rAF 同步 mock 断言)。typecheck + 3472 测试 + lint 绿。**沉淀:Modal 初始焦点应让调用方可声明意图(data-autofocus 显式标记),不能一刀切聚焦 first;React autoFocus 不可靠于「被框架接管初始焦点」的容器(会被 rAF 覆盖 + 不留 DOM 痕迹),用 data-* 属性更稳。**
- **A21 修(P2,A4 同族:Settings number/text/select 控件无 label 关联)**:`SettingItemRow.tsx:67` 通用渲染器的 number/text Input 与 select(SegmentedControl radiogroup)无 aria-label/labelledby,左侧设置标题仅视觉文本未建立关联 → AT 聚焦只读「spinbutton/edit text/radiogroup」不知编辑哪项。→ 标题 `<span>` 加稳定 `id={\`setting-title-${spec.id}\`}`;number/text Input 加 `aria-labelledby={titleId}`;design `SegmentedControl` 加可选 `ariaLabelledby`/`ariaLabel` prop(Continuo-local a11y 增强,radiogroup 须有可访问名)并由 SettingItemRow 传 titleId。boolean 的 switch 已在 A4 用 aria-label 命名,保持。+TDD(number/text/select 三类控件 aria-labelledby 指向标题且该 id 元素文本==标题;id 含 '.' 用属性选择器)。typecheck + 3475 测试 + lint 绿。**沉淀:表单控件可访问名收口须覆盖 boolean/number/text/select 全类型(A4 只修了 boolean);role=radiogroup 等 composite 控件须有 group-level 可访问名(aria-labelledby 指向可见标题),设计组件不支持就加可选 prop。**
- **A22 修(P2,A21 兄弟:另一 SegmentedControl 调用点缺 group 名)**:`EditorPanel.tsx:219` Markdown 模式切换 SegmentedControl(Edit/Source/Preview)的 radiogroup 未传 group 名 → AT 只读一组 radio 不知是「编辑器模式」选择器。这是 A21 后剩余唯一缺 group 名的 SegmentedControl 调用点。→ 传 `ariaLabel={t('panels.editor.mode_group')}`(复用 A21 给 design SegmentedControl 加的 ariaLabel prop)+ 新增 `panels.editor.mode_group` 三语言('Editor mode'/'编辑器模式'/'편집기 모드')。+TDD(radiogroup aria-label 本地化 == '编辑器模式')。typecheck + 3476 测试 + lint 绿。**沉淀:A21 加了 SegmentedControl 的 group-名能力,A22 全仓 grep `<SegmentedControl` 补齐所有调用点(SettingItemRow select + EditorPanel mode);composite 设计组件加可访问名 prop 后须 grep 所有调用点逐一传名(能力≠已应用)。** 注:codex 此轮前 context 自动压缩(12%→84%),续审有效。
- **A23 修(P2,不完整 ARIA tablist:声明 role 却无键盘模型)**:`TabNav.tsx:61` 用 `role="tablist"/"tab"`(编辑器/终端标签条)但所有 tab 默认可 Tab 聚焦、无 ArrowLeft/Right/Home/End 导航 → 键盘用户须逐个 Tab 穿过所有标签,屏幕阅读器进 tablist 后方向键不动焦点(不完整 WAI-ARIA tablist)。→ design 层补完整键盘模型(Continuo-local):TabNavItem 的 tab button `tabIndex={active ? 0 : -1}`(roving:仅 active 在 Tab 顺序);TabNav 的 tablist `onKeyDown` 处理方向键移焦(Home/End 首尾,左右循环,手动激活语义——方向键只移焦,Enter/Space 由原生 button 触发 onSelect)。+TDD(roving tabIndex [0,-1,-1] + 方向键/Home/End 移焦循环)。typecheck + 3477 测试 + lint 绿。**沉淀:声明了 role=tablist/tab/menu 等 composite role 就必须实现其 WAI-ARIA 键盘契约(roving tabindex + 方向键),否则是「半截 ARIA」比纯按钮更糟(承诺了交互模型却不兑现);要么补全要么降级为普通按钮(对比 A11 当时选不加 role=tab 正因不愿留半截)。**
- **A24 修(P2,A23 同族:SegmentedControl 声明 radiogroup/radio 却无键盘模型)**:`SegmentedControl.tsx:55` 用 `role="radiogroup"/"radio"` 但所有 radio 默认可 Tab、无 Arrow/Home/End → 半截 ARIA composite(同 A23)。→ design 层补完整 radio 键盘模型(Continuo-local):radio button `tabIndex={active ? 0 : -1}`(roving);radiogroup `onKeyDown` 方向键「自动激活」(radio group 标准:方向键同时移焦+切换 onChange,左右/上下循环,Home/End 首尾)。注:radiogroup 自动激活(arrow 即切选)区别于 A23 tablist 手动激活(arrow 仅移焦)。+TDD(roving [0,-1,-1] + 方向键自动激活 onChange 序列)。typecheck + lint + 全部 a11y/相关 spec 绿(3477 确定性通过)。**全量跑偶发 1-2 个失败均为 main.tsx 动态导入的重型 spike spec(thin-entry / bootstrap-locale-sync)在并发负载下的既有 flaky —— 隔离单跑全过,与本次叶子级 design 改动无关。** 沉淀:同一 design 组件 A21(group 名)→A22(调用点传名)→A24(键盘模型)三轮递进补全;composite role 的可访问名与键盘契约是两件独立的事,都要补。
- **A25 修(P2,composite tree 缺 group 名)**:`FolderTree.tsx:683` Explorer 主文件树 `tree.getContainerProps()`(headless-tree)未传 treeLabel → 渲染 `role="tree"` 且 `aria-label=""`(库实现:`aria-label: treeLabel ?? ''`)→ 屏幕阅读器进主文件树只感知无名 tree。→ 传 `tree.getContainerProps(tt('panels.explorer.tree_aria'))` + 新增 `panels.explorer.tree_aria` 三语言('File tree'/'文件树'/'파일 트리')。+TDD(spy 捕获 getContainerProps 实参 == t(tree_aria);因仅 items 非空才渲染 tree 容器,mock getItems 返非空、virtualizer 仍空不渲染 FileRow)。typecheck + lint + 目标 spec 绿(全量跑偶发失败仍为既有 flaky spike spec,隔离全过)。**沉淀:外部 headless 库的 composite role(tree/grid)同样要传可访问名 —— getContainerProps(treeLabel) 是库给的入口,默认空 label 等于无名;库管 ≠ 自动有 group 名。**
- **A26 修(P2,A5 同族:inline rename input 无可访问名)**:`FileRow.tsx:155` Explorer 重命名 `<Input {...item.getRenameInputProps()}>` 无可访问名(headless-tree getRenameInputProps() 只给 ref/onBlur/value/onChange,无 aria-label)→ 键盘/屏幕阅读器进重命名编辑框只听到无名 textbox。→ 调用点补 `aria-label={t('panels.explorer.rename_input_aria', { name: data.name })}`(静态 t,rename 短暂不需每行订阅 locale)+ 新增 `panels.explorer.rename_input_aria` 三语言(含 `{name}` 插值)。+TDD(isRenaming=true 时 input aria-label 含文件名,locale-无关)。typecheck + lint + 目标 spec 绿(全量偶发失败仍为既有 flaky spike spec,隔离全过)。**沉淀:headless 库给的 props(getRenameInputProps/getRenameInputProps 类)常不含可访问名,须在调用点补 aria-label —— 与 A25 tree 同理:库管的可交互元素也要自查可访问名。**
- **A27 修(P2,焦点还原:Radix ContextMenu 全局禁用 onCloseAutoFocus)**:`ContextMenu.tsx:165` 全局 `onCloseAutoFocus={(e) => e.preventDefault()}` 无条件禁用菜单关闭后的焦点还原 → 键盘用户 Esc/普通项(cut/copy/paste/trash/reveal/插件项)关闭后焦点不回触发行(丢到 body)。**但此 preventDefault 是有意的**:新建/重命名会随后打开 CreateInput/RenameInput 并 rAF 自聚焦,Radix 默认还原会抢回焦点 → 简单删除会回归该 bug。→ 抽 `menu-close-focus.ts` 一次性 `skipOnce()`+`onCloseAutoFocus`:默认保留 Radix 还原,仅「新建文件/新建文件夹/重命名」三项 onSelect 调 skipOnce 跳过一次。+TDD(默认不 preventDefault / skipOnce 后 preventDefault 一次 / 一次性后恢复默认)。typecheck + helper spec + lint 绿。**沉淀:框架默认 a11y 行为(Radix 焦点还原)被全局绕过往往是为某条特殊路径(自聚焦输入)—— 修法不是删除绕过(会回归原 bug),而是把绕过收窄到那条路径(一次性 skip ref),其余保留框架默认。读懂「为何加这个 preventDefault」是关键(注释救了我)。**
- **A28 修(P2,A23 兄弟:design Tabs 组件声明 tablist/tab 无键盘模型)**:`Tabs.tsx:33` 导出的共享 Tabs(纵向 tab 列表)声明 `role="tablist"/"tab"` 但无 roving tabindex / 无方向键(同 A23,任何调用方都得不完整 tablist;当前虽无生产直接用,但是导出组件=潜在缺口)。→ 抽共享 `src/design/roving-tablist.ts handleTablistArrowKeys`(支持横/纵向:Left/Up=prev、Right/Down=next、Home/End、循环),Tabs 接入 + tab button roving tabindex;**并把 A23 的 TabNav 内联逻辑重构为复用同一 helper(单一来源,修一族必修兄弟反向收口)**。+TDD(Tabs roving [0,-1,-1] + ArrowDown/Up/Home/End 循环;TabNav A23 测试仍绿)。typecheck + 20 测试 + lint 绿。**沉淀:design 层导出的 composite 组件即使当前无生产调用也要补全 ARIA 键盘契约(导出=契约面);A23/A28 两个 tablist 收口为单一 handleTablistArrowKeys helper。**
- **A29 修(P2,A23 后续:tablist 内 close 按钮仍占 Tab 顺序)**:`TabNav.tsx:128` A23/A28 让 `role="tab"` 做了 roving,但每个 tab 的 close 按钮仍默认 `tabIndex=0` 且在 `role="tablist"` 内 → 键盘用户仍逐个 Tab 穿过关闭按钮,tablist 混入非 tab 的额外 tab stop,破坏「一次 Tab 进出 composite + 方向键在 tab 间移动」模型。→ close 按钮 `tabIndex={-1}`(移出 Tab 顺序,鼠标仍可点)+ tab select 按钮加 `onKeyDown` Delete/Backspace → onClose(WAI-ARIA「可删除 tab」标准:聚焦 tab 后删除键关闭)。+TDD(close tabIndex=-1、tab 上 Delete/Backspace 各触发 onClose、普通键不触发;editor-header 点击关闭仍绿)。typecheck + 29 测试 + lint 绿。**沉淀:composite(tablist)的 roving tabindex 不只管「主项(tab)」—— 项内的附属交互(close 按钮)也会成为破坏键盘模型的额外 tab stop,须一并移出 Tab 顺序并提供替代键盘入口(Delete/Backspace)。roving 是「整个 composite 只一个 tab stop」的契约,附属控件别漏。**
- **A30 修(P2,A18/A27 同族:自写 menu 项激活后不还原焦点)**:`ExplorerHeader.tsx` 自写 `role="menu"` 的普通菜单项(展开全部/打开最近/切换文件夹/关闭文件夹)激活后只 `setMenuOpen(false)`,菜单项随菜单卸载 → 键盘 Enter 激活后焦点落到被卸载元素/body。A18 hook 只在 Escape 路径还原焦点,菜单项激活路径漏了。→ `use-menu-keyboard.ts` 扩展导出 `closeAndRestore()`(onClose + triggerRef.focus(),Escape 路径也复用),ExplorerHeader 4 个普通项改用它(关闭文件夹会卸载组件 → triggerRef 空 focus no-op)。**HeaderActions(dock)同模式但其菜单项是「添加 panel/terminal → dockview 聚焦新面板」即会主动聚焦新目标,按 codex 准则保留前向焦点不还原,故不改。** +TDD(点「展开全部」后菜单关闭且 activeElement==触发按钮)。typecheck + 24 测试 + lint 绿。**沉淀:菜单焦点还原有两条路径 —— Escape(A18 已覆盖)和菜单项激活(A30);菜单项激活分两类:不移动焦点的(还原到 trigger)vs 主动聚焦新目标的(保留前向,如 dockview 新面板),按 callsite 判断,别一刀切。**
- **A31 修(P2,A29 同族 for menu:MenuItem 仍占 Tab 顺序)**:`MenuItem.tsx:33` 自写 role=menu 的 MenuItem 默认 `tabIndex=0` → 打开菜单后键盘 Tab 逐个穿过每个菜单项,破坏 menu composite(箭头移动、Tab 离开)。MenuItem 恒 `role="menuitem"` → 按 ARIA 应恒 `tabIndex=-1`(焦点由 useMenuKeyboard 程序管理 initial focus+Arrow/Home/End,-1 不影响 .focus())。→ MenuItem `tabIndex={-1}` + `use-menu-keyboard.ts` onKeyDown 处理 Tab/Shift+Tab → closeAndRestore(WAI-ARIA menu:Tab 关闭菜单,避免焦点 leak 到 portal 之后 DOM)。+TDD(MenuItem tabIndex=-1;ExplorerHeader 菜单项全 -1 + Tab 关闭菜单还原焦点)。typecheck + 43 测试 + lint 绿。**沉淀:A29(tablist)+A31(menu)同根 —— 凡声明 composite role(menu/tablist/listbox 等)的子项都须 tabIndex=-1(roving),整个 composite 只一个 Tab 入口,Tab 离开;子项焦点全程程序管理。MenuItem/SegmentedControl/TabNavItem 三个 design 子项组件现都已 roving。**
- **A32 修(P2,错误 ARIA 状态过度暴露:NavRailButton 无条件 aria-pressed)**:`NavRailButton.tsx:35` 无条件输出 `aria-pressed={active}`(默认 active=false),但该组件也用于非 toggle 的 ribbon action / 命令按钮(IconSidebar ribbonActions 不传 active)→ AT 把普通动作按钮读成「未按下的切换按钮」,误导有 pressed 状态。→ `active` 改真可选(无默认),`aria-pressed={active === undefined ? undefined : active}`:调用点明确传 active 的真实 toggle/destination(IconSidebar renderItem nav 项)才暴露 aria-pressed,ribbon/命令按钮不传 → 不渲染。data-active/active-bar 仍按 active===true 渲染。+TDD(不传 active 无 aria-pressed;传 active={false} 有 aria-pressed=false)。typecheck + 19 测试 + lint 绿。**沉淀:aria 状态属性(aria-pressed/checked/selected/expanded)是「承诺该控件有此状态」—— 不能对所有实例无条件输出;多用途共享组件须让状态属性随「是否真有该状态」可选(传才渲染),否则非 toggle 控件被误标。这是「缺状态」(A8/A11/A16)的反面:多了不该有的状态。**
- **A33 修(P2,A32 调用点传播:IconSidebar `?? false` 抵消 A32)**:`IconSidebar.tsx:86` A32 把 NavRailButton.active 改可选后,renderItem 仍 `active={item.active ?? false}` 把未声明状态的命令按钮(Settings 齿轮,无 active 字段)强转 false → 仍渲染 `aria-pressed="false"`,A32 组件侧修复没传播到调用侧。→ 条件传递 `{...(item.active === undefined ? {} : { active: item.active })}`:真 toggle(explorer=sidebarOpen)传 active 暴露 aria-pressed,命令按钮(settings)不传则不渲染。+TDD(Settings 无 aria-pressed、Explorer toggle 有 aria-pressed=true)。typecheck + 14 测试 + lint 绿。**沉淀:组件侧把状态属性改「可选」后,调用侧的 `?? false`/`?? defaultState` 默认值会把「无状态」coalesce 成「有状态(假)」,抵消组件修复 —— 改组件 optional 状态属性时必 grep 所有调用点的默认值合并,确保「无状态」一路透传为 undefined(同 A22/A24「能力≠已应用」,这里是「默认值偷偷重新引入」)。**
- **A34 修(P2,角标 aria-label 未与按钮关联)**:`IconSidebar.tsx:128` Settings 齿轮的更新角标把 `aria-label="N 个插件可更新"` 放在独立非交互 span 上,未与可聚焦的 Settings 按钮关联 → 键盘/AT 聚焦按钮只听到「设置」,听不到更新数。→ NavRailButton 加可选 `describedById` → `aria-describedby`;角标 span 加稳定 id(`SETTINGS_UPDATE_BADGE_ID`),settings 分支 `renderItem(item, BADGE_ID)` 关联。顺带修 `topItems.map(renderItem)` → `map((item) => renderItem(item))`(避免 map index:number 传给新增的 describedById:string 第2参 typecheck 报错)。+TDD(Settings 按钮 aria-describedby 指向角标 id)。typecheck + 14 测试 + lint 绿。**沉淀:状态信息(角标/计数)放在交互元素旁的独立元素上,AT 聚焦该交互元素时读不到 —— 须用 aria-describedby(补充描述)或并入 aria-label 把信息关联到可聚焦控件。另:给 .map(fn) 直接传函数会隐式转发 index,新增可选第2参且类型非 number 时会触发 typecheck,改成显式 arrow 包裹。**
- **A35 修(P2,dirty tab 状态未暴露给 AT)**:`TabNav.tsx:140` dirty tab 只用 `aria-hidden` 的视觉圆点表示未保存,EditorHeader 传 dirty 后未把「未保存」关联到 role=tab 可访问名/描述 → AT 聚焦编辑器标签只听文件名,不知有未保存内容。→ TabNavItem 加可选 `dirtyLabel`(调用方传本地化,design 层无 i18n);dirty 且有 dirtyLabel 时复用圆点 span(加 `id`+`aria-label={dirtyLabel}`、去掉 aria-hidden)+ tab 按钮 `aria-describedby` 指向它;无 dirtyLabel 则圆点仍 aria-hidden(向后兼容)。EditorHeader 传 `dirtyLabel={t('panels.editor.unsaved_indicator')}` + 新增三语言。+TDD(dirty+dirtyLabel→describedby 指向 aria-label 文本;clean/无 dirtyLabel→无 describedby 且圆点 aria-hidden)。typecheck + 33 测试 + lint 绿。**沉淀:纯视觉状态指示(dirty 圆点/角标/颜色)对 AT 不可见,须把状态文本关联到可聚焦控件(aria-describedby/label);design 层用「调用方传本地化文本 + 组件只接 prop」绕开 design-no-i18n 铁律(同 A16 Spinner/A21 SegmentedControl 模式)。**
- **A36 修(P2,A1 同族:图标-only 链接无可访问名)**:`MarketplaceTab.tsx:837` ReviewItem 的 GitHub 外链是 icon-only(内容仅 `↗`)+ title,无 aria-label → AT 聚焦可访问名为箭头符号,不知是「在 GitHub 打开评论」。→ 加 `aria-label={t('marketplace.reviews.open_in_github')}`(复用已有 key,title 保留作鼠标提示)+ ↗ 包 `aria-hidden`。+TDD(展开 reviews 后该 `<a>` aria-label 非仅箭头)。typecheck + 26 测试 + lint 绿。**沉淀:可访问名缺失族不止 button —— icon-only 的 `<a>` 链接同样须 aria-label(title 是鼠标 tooltip,非可靠可访问名);A1 起的「图标-only 交互元素无名」族覆盖 button + IconButton + a 链接。**
- **A37 修(P2,A35 同族:StatusBar dirty 圆点无 AT 文本)**:`StatusBar.tsx:204` active editor dirty 只在文件名后渲染视觉 ● 无文本 → AT 浏览状态栏只听文件名后一个符号,不知未保存。→ ● 标 `aria-hidden` + 追加视觉隐藏的真实文本 `t('statusbar.unsaved_changes')`(状态栏是静态文本流,aria-label on 普通 span 在浏览模式不可靠 → 用真实文本;项目无 sr-only 类 → 内联样式自足)+ 新增 `statusbar.unsaved_changes` 三语言。+TDD(dirty→textContent 含「未保存的更改」且 ● aria-hidden;clean→无)。typecheck + 21 测试 + lint 绿。**沉淀:dirty 状态有两个呈现点(A35 编辑器 tab + A37 状态栏),都须给 AT 文本但机制不同 —— tab 有可聚焦控件用 aria-describedby;状态栏是静态文本流须用视觉隐藏的真实文本(非 aria-label,浏览模式才可靠读出)。同一状态的不同呈现点是「修一族必修兄弟」,但每处的关联机制要按其是否可聚焦/是否文本流分别选。**
- **A38 修(P2,A37 同族:单条 review 星级纯视觉符号无 AT 文本)**:`MarketplaceTab.tsx:827` ReviewItem 评分只渲染 ★/☆ 视觉符号,无 aria-label → AT 只听一串星号,不知几星。聚合评分(line 720)已有 `aria-label={t('marketplace.reviews.stars_aria',{avg})}` 的既定模式,但单条 review 漏了。→ 镜像同款 aria-label(`avg: String(r.rating)`)。+TDD(展开后带 aria-label 的星级 span ≥2 = 聚合 + 单条)。typecheck + 26 测试 + lint 绿。**沉淀:同一视觉模式(星级)在聚合处已做对、单条处漏 —— 是「修一族必修兄弟」的纯视觉状态版:有既定正确模式时镜像它,别新造。纯符号(★/👍/●/↗/箭头)= AT 噪声,凡承载语义的符号都须 aria-label/隐藏文本。**
- **A39 修(P2,A38 同族:thumbs-up emoji+计数无 AT 文本)**:`MarketplaceTab.tsx:829` 单条 review 点赞数只显示 `👍 {count}` + title,语义靠 emoji+title,无真实可读文本 → AT 只听表情符号+数字。→ `👍 {count}` 包 `aria-hidden` + 追加视觉隐藏的本地化文本 `t('marketplace.reviews.thumbs_up',{count})`(文本流里 aria-label on span 浏览模式不可靠 → 真实隐藏文本,同 A37;加本文件 SR_ONLY 内联样式)。+TDD(👍 在 aria-hidden span;sr-only span position:absolute 含计数)。typecheck + 27 测试 + lint 绿。**沉淀:emoji 承载语义(👍 点赞/⚠ 警告)= 视觉符号同族,在文本流里须 emoji aria-hidden + 视觉隐藏真实文本(A37/A39 用 SR_ONLY 内联样式,因项目无 sr-only 类——这是第 3 处复制,若再现应抽共享 sr-only 组件/样式)。**
- **A40 修(P2,A35/A37 同族:TitleBar dirty 圆点无 AT 文本 + 抽共享 sr-only)**:`TitleBar.tsx:35` 窗口标题栏 dirty 拼成纯视觉 ` ●`(嵌在 `text` 字符串里)无可读文本 → AT 浏览标题栏只听文件名后符号。dirty 第 3 个呈现点(A35 tab/A37 状态栏/A40 标题栏)。→ 把标题从单一拼接字符串重构为结构化 JSX:fileName + (dirty ? `<span aria-hidden> ●</span>` + sr-only `t('statusbar.unsaved_changes')`) + sep + wsLabel + fallback;**抽共享 `src/lib/sr-only.ts SR_ONLY_STYLE`,StatusBar/MarketplaceTab/TitleBar 三处统一改用(单一来源,删两处重复内联样式)**。复用既有 statusbar.unsaved_changes key。+TDD(dirty→含未保存文本 + ● aria-hidden;clean→无;注:TitleBar useMemo 失效键 chromeVersion 须随 dirty bump)。typecheck + 54 测试 + lint 绿。**沉淀:同一状态的呈现点可能散布多处(dirty 共 3 处),codex 逐个报、逐个修是「修一族必修兄弟」的长尾;第 3 次重复同一 helper(SR_ONLY)即抽共享单一来源。把拼接字符串里的语义符号分离出来须重构为结构化 JSX(字符串内无法 aria-hidden 局部)。**
- **A41 修(P2,live region 缺失:异步错误不播报)**:`MarketplaceTab.tsx:353` reviews 刷新失败是异步插入的 inline error 文本,但容器无 `role="alert"`/aria-live → 用户点刷新后焦点仍在按钮,失败信息插入页面 AT 不主动播报。→ 给错误容器加 `role="alert"`(隐式 aria-live=assertive)。+TDD(error 非空时 [role=alert] 存在且含错误文本)。typecheck + 27 测试 + lint 绿。**沉淀:新类「异步状态变化(error/success/loading 完成)不在 live region」—— 动态插入的反馈文本若不在 role=alert/status 或 aria-live 容器内,屏幕阅读器用户(焦点未移到该文本)完全感知不到。凡「操作后异步出现的反馈文本」都须 live region。**
- **A42 修(P2,A41 同族:Plugins 安装/卸载结果无 live region)**:`PluginsTabContent.tsx:347` Git URL 安装/卸载结果 installMsg 异步插入但容器无 role/aria-live → AT 不播报。installMsg 原是纯字符串无严重度 → 改结构化 `{text, isError}`(5 个 setInstallMsg 站点标 isError),渲染按语义选 live region:失败 `role="alert"`(assertive)、成功 `role="status"`(polite)。+TDD(成功→[role=status] 含成功文本;失败→[role=alert] 含错误码)。typecheck + 28 测试 + lint 绿。**沉淀:live region 须按严重度分 role —— 错误用 alert(assertive 打断),成功/进度用 status(polite 不打断);单一 message 状态要承载严重度(改 {text,isError})才能在渲染处正确选 role。A41(Marketplace error)+A42(Plugins install/uninstall)同族,凡异步反馈都查 live region。**
- **A43 修(P2,A42 同族:Marketplace Git URL 安装结果无 live region)**:`MarketplaceTab.tsx:465` Git URL 安装结果 msg 异步插入但普通 `<div>` 无 live region。同 A42:`msg` 改结构化 `{text,isError}`(4 个 setMsg 站点标记),渲染失败 `role="alert"`/成功 `role="status"`。+TDD(成功→含安装文本的 role=status;失败→含错误文本的 role=alert;用 .some() 定位,因页面另有 A41 reviews alert)。typecheck + 27 测试 + lint 绿。**沉淀:live region 族(A41 reviews error / A42 Plugins install / A43 Marketplace install)同根,凡异步插入的成功/失败反馈都须 status/alert;测试断言 live region 须用 .some() 按文本定位(同页可能多个 alert/status)。安装结果反馈有两个面板入口(Plugins tab + Marketplace tab)= 修一族必修兄弟。**
- **A44 修(P2,A42/A43 同族:Marketplace 卡片级安装/更新结果无 live region)**:`MarketplaceTab.tsx:629` 卡片级 message(`install.msgs` Map per-entry)异步插入普通 `<div>` 无 live region。onInstall/onUpdate 已跟踪 `success` → msgs map 值从 `string` 改 `{text,isError}`(两 handler setter + 类型 + 卡片 prop 类型 + 渲染按 role=alert/status)。+TDD(卡片安装成功→role=status 含安装文本;失败→role=alert 含错误码)。typecheck + 27 测试 + lint 绿。**沉淀:安装结果反馈共 4 个入口(A42 Plugins tab git-url / A43 Marketplace git-url / A44 Marketplace 卡片 install+update)—— live region 族在同一 codex 方向连出 4 个兄弟,凡「异步 success/error 反馈」一律 status/alert;per-entry Map 状态也要把值升级成带严重度结构。**
- **A45 修(P2,A41 同族:QuickOpen 扫描失败提示无 live region)**:`QuickOpenModal.tsx:216` Quick Open 扫描失败提示是异步插入的 inline error,容器无 role=alert → 焦点在搜索框时 AT 不播报。→ 失败态容器加 `role="alert"`。+TDD(walk 失败 scanFailed 后 [role=alert] 存在且含失败文案+重试按钮)。typecheck + 15 测试 + lint 绿。**沉淀:live region 族延伸到第 5 处(A41/A42/A43/A44/A45)—— Marketplace reviews error + 4 处安装结果 + QuickOpen 扫描失败;凡「异步出现且焦点不在其上」的失败/状态文本都须 role=alert(失败)或 status(成功/中性)。这是 codex 在单一方向里把同一缺陷类穷尽到所有入口的长尾收口。**
- **A46 修(P2,保存失败静默无反馈)**:`PermissionEditorModal.tsx:78` 权限编辑保存失败只 `console.error` 并保持 Modal 打开,无任何可见/live 反馈 → 用户点保存看似无响应,AT 也不知失败。→ 加 `saveFailed` 状态(save 开始清、catch 置位),失败时渲染 `role="alert"` 的本地化提示(新增 `permissions.editor.save_failed` 三语言),Modal 仍保持打开重试。+TDD(store.grant reject → role=alert 出现 + onClose 未调 + Modal 未关)。typecheck + 7 测试 + lint 绿。**沉淀:live region 族新变体 —— 不仅「已有反馈文本缺 live role」(A41-A45),还有「失败完全无反馈只 console.error」(A46):静默失败对所有用户都是 bug,对 AT 更甚;凡 catch 里只 console.error 的用户触发操作都要补可见+live 反馈。**
- **A47 修(P2,A46 同族:Plugins 生命周期操作失败只 console.warn)**:`PluginsTabContent.tsx` reload/disable/enable/retry 四个按钮的 catch 都只 `console.warn` + refresh(),无可见/live 反馈 → 用户点击后界面看似无响应。→ 抽 `reportActionError(err)`(按 code 本地化经 localizeErrorByCode)统一路由到 A42 的 installMsg live region(role=alert),4 个 catch 各加一行调用(保留 console.warn 作日志)。+TDD(enable reject → role=alert 含错误文本)。typecheck + 28 测试 + lint 绿。**沉淀:静默失败族(A46/A47)—— 同一面板多个用户触发的生命周期操作 catch 全是「只 console.warn」,统一抽 reportActionError 复用已建的 live region(A42 installMsg),一处机制覆盖 reload/disable/enable/retry 全兄弟;修一族必修兄弟在同文件多 handler 时优先抽统一 helper 而非逐个内联。**
- **A48 修(P2,A46/A47 同族:Explorer Copy Path 剪贴板失败静默)**:`FolderTree.tsx:326` Copy Path / Copy Relative Path 写剪贴板失败只 console.warn,无 toast/live 反馈 → 用户以为已复制成功。→ 抽 `copy-to-clipboard.ts copyToClipboardOrNotify(text, failMessage)`(失败时 notify.error toast + 保留 console.warn),两个调用点统一改用,失败文案 `panels.explorer.copy_path_failed` 三语言。+TDD(helper:写成功不打扰、reject→notify.error 本地化文案;FolderTree spec 无回归)。typecheck + helper 2 测试 + lint 绿。**沉淀:静默失败族(A46 权限保存/A47 插件生命周期/A48 复制路径)第 3 类入口 —— 剪贴板写;凡 catch 只 console.warn 的用户触发操作都要补 notify/live 反馈,FolderTree 这类难端到端测的内联闭包抽小 helper 单测 + DRY 两调用点(同 A47 reportActionError 思路)。**
- **A49 修(P2,A47/A48 同族:Explorer Reveal in Finder 失败静默)**:`FolderTree.tsx:342` onRevealInFinder 在 `!r.ok` 时只 console.warn,无 toast/live 反馈 → 用户只见菜单关闭不知 reveal 失败。→ 抽 `reveal-or-notify.ts revealPathOrNotify(path)`(失败 notify.error 按 code 本地化 + 保留 console.warn),FolderTree 调用点改用。+TDD(helper:ok 不打扰、ok=false→notify.error 本地化;FolderTree spec 无回归)。typecheck + 5 测试 + lint 绿。**沉淀:静默失败族(A46-A49)第 4 类入口=OS reveal;Explorer 上下文菜单的 fs 副作用 handler(copy/reveal)凡 catch/!r.ok 只 console.warn 都补 notify;内联闭包统一抽小 helper(copy-to-clipboard/reveal-or-notify)单测。onOpenInTerminal/trash/paste 本已有 notify(兄弟入口先前已对)。**
- **A50 修(P2,A46-A49 同族:Dock popout 失败无反馈)**:`HeaderActions.tsx:100` popout 按钮 `void containerApi.addPopoutGroup(...)`(返回 Promise<boolean>)直接丢弃异常,无 catch/反馈 → 弹出失败时按钮无响应、AT 不知失败。→ `.catch` 后 `notify.error(t('shell.dock.popout_failed'))` + console.warn,新增三语言 key。测试侧:makeProps 默认 addPopoutGroup 返回 `Promise.resolve(true)`(真实就是 Promise,原 mock 返 undefined 会让 .catch 抛)。+TDD(addPopoutGroup reject → notify.error)。typecheck + 9 测试 + lint 绿。**沉淀:静默失败族(A46-A50)第 5 类入口=直接 `void promise` 丢弃异常(非 catch-only-warn)—— `void someAsync()` 同样吞掉 reject,用户触发的异步操作即便用 void 也要 `.catch + notify`。改调用点加 .catch 后,测试 mock 必须返回真 Promise(否则 .catch 抛),改组件 promise 处理必同步修 mock。**
- **A51 修(P2,A41 同族:StatusBar MCP 复制结果不在 live region)**:`StatusBar.tsx:119` MCP 配置复制结果只靠按钮文本短暂变化 1.5s 表示(copied/unavailable/failed),按钮无 role=status/aria-live → 焦点停在按钮上时状态变化不一定播报。→ 加视觉隐藏 `role="status"`(polite)live region span 镜像结果(仅非 idle 渲染文本,idle→空不打扰),状态变化时 AT 播报;按钮文本不动(视觉不变)。复用 SR_ONLY_STYLE。+TDD(复制成功后 [role=status] 含「已复制」)。typecheck + 21 测试 + lint 绿。**沉淀:live region 族延伸到「瞬时状态(按钮文本短暂变化)」—— 即便有可见文本变化,若它在交互元素自身且非 live region,焦点在该元素的 AT 用户不一定听到;用独立视觉隐藏 status 镜像瞬时结果(仅非 idle 渲染避免持续噪声)。**
- **A52 修(P2,A41 同族:Keybinding 冲突警告不在 live region)**:`KeybindingCaptureModal.tsx:154` 快捷键捕获后动态出现的冲突警告无 role/aria-live → 焦点在弹窗内的 AT 用户听不到冲突,可能保存已冲突的快捷键。→ 冲突容器加 `role="status"`(冲突是可覆盖的警告 → polite,非 alert)。+TDD(捕获 mod+x 与已有命令冲突 → [role=status] 含冲突命令名)。typecheck + 15 测试 + lint 绿。**沉淀:live region 族第 8 处(A41/A42/A43/A44/A45/A51/A52 + A46-A50 静默失败补反馈);live role 选择按语义:错误/阻断用 alert(assertive),可覆盖警告/成功/中性进度用 status(polite)—— 冲突警告允许保存故 polite。**
- **A53 修(P2,A41 同族:Terminal 搜索计数不在 live region)**:`TerminalSearchBar.tsx:86` 匹配计数/「无匹配」随输入和上下跳转动态变化,计数 span 无 role/aria-live → 焦点停在搜索框时 AT 不知匹配数/当前第几项。→ 计数 span 加 `role="status"`(polite,持续输入的中性进度)。+TDD(计数文本 closest [role=status] 非空)。typecheck + 7 测试 + lint 绿。**沉淀:搜索/过滤结果计数是「输入即变的中性进度」live region 典型场景(focus 锁在输入框,结果在别处)→ role=status/polite;与 A51 瞬时按钮状态、A52 冲突警告同属 polite 类。live region 族已覆盖 error(alert)/success(status)/警告(status)/瞬时(status)/计数(status)全谱。**
- **A54 修(P2,A53 同族:Settings 搜索结果计数不在 live region)**:`SettingsPanel.tsx:211` 搜索结果摘要(匹配 N 项/未找到匹配)随输入变化但无 role/aria-live → 焦点在搜索框时 AT 不知匹配数/无结果。→ 结果摘要 div 加 `role="status"`(polite)。+TDD(搜索后 [role=status] 含匹配摘要)。typecheck + 19 测试 + lint 绿。**沉淀:搜索结果计数 live region 是「修一族必修兄弟」—— A53(Terminal 搜索)+A54(Settings 搜索)同模式;凡「输入框搜索 + 结果摘要在别处」的面板都查。Marketplace 搜索结果摘要(若有)也应核(下一兄弟候选)。**
- **A55 修(P2,A53/A54 同族:Marketplace 结果摘要不在 live region)**:`MarketplaceTab.tsx:331` 搜索/标签过滤结果摘要(shown/total)随输入和 tag toggle 变化但无 role/aria-live → 焦点在搜索框/tag 按钮时 AT 不知筛选结果数量。→ 摘要 span 加 `role="status"`(polite)。+TDD([role=status] 含「显示 2 / 共 2」)。typecheck + 27 测试 + lint 绿。**沉淀:搜索/过滤结果计数 live region 三兄弟收口(A53 Terminal / A54 Settings / A55 Marketplace)—— 我在 A54 预判 Marketplace 是下一兄弟,codex 第 55 轮即报,印证「修一族必修兄弟」可前瞻 grep 同模式入口。live region 族已极完整(error/success/警告/瞬时/计数 × 各面板)。**
- **A56 修(P2,A53-A55 同族:combobox/列表空态不在 live region)**:`CommandPalette.tsx:205` combobox 过滤为空时空态文本无 role/aria-live → 焦点锁搜索框、输入致从有结果变无结果时 AT 不知「无匹配」。→ 空态 div 加 `role="status"`(polite)。**按「修一族必修兄弟」顺手修 QuickOpenModal 同模式空态(empty/no_match)**(combobox 兄弟,A15 起一直成对)。+TDD(CommandPalette + QuickOpen 空态各在 [role=status])。typecheck + 29 测试 + lint 绿。**沉淀:live region「无结果空态」是搜索计数族的补集 —— 有结果时计数 status(A53-A55),无结果时空态也须 status(A56);combobox 双子(CommandPalette + QuickOpen)凡空态/计数一律成对修。live region 族至此覆盖 error/success/警告/瞬时/计数/空态全场景 × 全面板。**
- **A57 修(P2,A54/A56 同族:Keybindings 搜索空态不在 live region)**:`KeybindingsTabContent.tsx:168` 搜索无结果空态(empty/no_match)随输入出现但无 role/aria-live → 焦点锁搜索框时 AT 不知已无匹配。→ 空态容器加 `role="status"`(polite)。+TDD(输入 zzz 无匹配 → [role=status] 含「无匹配命令」)。typecheck + 16 测试 + lint 绿。**沉淀:搜索空态 live region 第 3 处(A56 CommandPalette/QuickOpen + A57 Keybindings);Settings(A54)/Marketplace(A55)用 count_summary 在空时显「显示 0/共 N」role=status 已覆盖空态(不另加 no_match status 避免双播报)。codex 此轮放弃 tree-row badge 候选(headless-tree treeitem 由库命名,焦点行 name 已含文件名,badge 是次要装饰非误报)转报此项。**
- **A58 修(P2,A50 同族:AgentAuthPrompt respond 丢弃异常无反馈)**:`AgentAuthPrompt.tsx:43` 授权弹窗点允许/拒绝后 `void coApi.agentAuth.respond(...)`(返回 `Promise<IpcResult<void>>`,可 !ok 或 reject)直接丢弃 → IPC 应答失败时弹窗已关、main pending 等 5 分钟才默认拒绝,用户/AT 不知决定未生效(安全敏感:授权操作静默丢失)。→ try/catch + 检查 `!r.ok`,失败 `notify.error(translate('permissions.agent.respond_failed'))`(静态 translate,异步回调取调用时 locale)+ console.warn,新增三语言。+TDD(respond ok=false → notify.error;默认 mock 改返 {ok:true} 匹配真实 IpcResult)。typecheck + 8 测试 + lint 绿。**沉淀:void promise 静默失败(A50 popout/A58 agent-auth respond)在安全/授权链路更严重——决定丢失但 UI 假装已处理;凡 `void coApi.*` 的用户决定应答都须检查 IpcResult.ok + catch。**
- **A59 修(P2,A46 同族 load 侧:PermissionEditorModal 加载 decisions 失败静默)**:`PermissionEditorModal.tsx:52` 加载现有权限 decisions 失败只 console.error,无可见/live 反馈 → 用户看到空/默认控件态、AT 不知未加载,点保存可能用错误空态覆盖已存权限。A46 修了 save 侧,这是 load 侧。→ 加 `loadFailed` 状态(load 始清/catch 置位 + cancelled 守卫),失败渲染 `role="alert"` 本地化提示(新增 `permissions.editor.load_failed` 三语言)+ **保存按钮 `disabled={loadFailed}`**(防覆盖)。+TDD(store.get reject → role=alert + Save disabled)。typecheck + 8 测试 + lint 绿。**沉淀:静默失败有 save 侧也有 load 侧(A46 save/A59 load)——同一弹窗的读+写两条 IPC 都要失败反馈;load 失败额外要禁用后续写操作(数据安全:不让用户用未加载的空态覆盖已存数据,与本仓 topic-54 数据安全「读失败当空→写覆盖」族同根)。**
- **A60 修(P2,live region 族根因:Toast 错误固定 role=status)**:`Toast.tsx:20` 统一 Toast 组件固定 `role="status"`,只靠 `aria-live="assertive"` 区分错误 → AT 把 `notify.error` 失败也当普通状态消息(role 与 live 语义不一致;且 role=status 隐式 aria-live=polite 与显式 assertive 冲突)。**这是 A46-A50/A58 全部「补 notify.error 反馈」修复所依赖的可访问出口本身的语义缺陷。**→ 按 level 切换 `role={level === 'error' ? 'alert' : 'status'}` + `aria-live` 同步(error→assertive,其余→polite)。+TDD(error toast → [role=alert] 含消息+code;非错误 info → [role=status] 且无 alert;同页 error 再现 → alert)。更新既有 T12(getByRole('status')→getByRole('alert'))。typecheck + 25 测试 + lint 绿。**沉淀:live region 族(A41-A59)层层补反馈出口,A60 是其根 —— 出口组件(Toast)自身 role 固定不分严重度,则所有经它的 notify.error 在 AT 里都降级为普通状态;补反馈不只看「有没有 live region」,还要看「live region 的 role/severity 是否随消息级别正确切换」。统一组件的语义正确性是所有调用点正确性的前提。**
- **A61 修(P2,A52 同族:KeybindingCaptureModal 捕获结果框本身不在 live region)**:`KeybindingCaptureModal.tsx:132` 捕获结果框随每次按键在「按下新组合」/组合键 KeyCap/「未绑定」三态间动态切换,但按键由 `document.addEventListener(keydown, ..., true)` 全局捕获处理、焦点不在该框上,框无 role/aria-live → AT 用户按下组合后听不到是否已捕获/清空,只能盲目保存。A52 修的是冲突警告(line 154),这是捕获结果框本身(不同区域)。→ 给捕获结果框加 `role="status"`(捕获结果是中性进度 → polite 不打断)。+TDD(占位「按下新组合」/按 ctrl+x→组合含 X/Backspace→「未绑定」三态均在 role=status 内;A52 冲突测试因同页现两个 role=status 改用 `.some()` 按文本定位)。typecheck + 16 测试 + lint 绿。**沉淀:同一弹窗内有两个独立动态区域(捕获结果框 + 冲突警告)各需自己的 live region —— A52 只修了警告,捕获结果框这个「用户操作的主反馈」反而漏了;document-capture 键盘交互(焦点不随按键移动)的动态显示区尤其需要 live region。加第二个 role=status 后,原 querySelector('[role=status]') 单数选择器会改命中 DOM 靠前者,须改 .some() 按文本定位(同 A43/A55)。**
- **A62 修(P1,键盘陷阱:KeybindingCaptureModal 拦截 Tab 当组合)**:`KeybindingCaptureModal.tsx:86` 全局 `document.addEventListener(keydown,...,true)` handler 只放行 Escape/Backspace,其余键交给 `eventToCombo`;Tab(含 Shift+Tab)非修饰键 → 被编成 'tab' 组合并 `preventDefault` → 弹窗内键盘用户无法用 Tab 在 取消/重置/保存 间移动焦点(捕获组合后 Save 启用却到达不了),形成键盘操作断点。这是首个 **P1** a11y 项(前面 A16-A61 多为 P2 语义/反馈)。→ handler 中 `if (e.key === 'Tab') return;`(与 Escape 同级提前返回),放行 Modal focus trap / 浏览器默认 Tab 焦点导航;Shift+Tab key 仍为 'Tab' 故同一检查覆盖反向导航。+TDD(按 Tab:defaultPrevented=false + 未捕获组合(Save 仍 disabled、占位文案在);Shift+Tab 同理)。typecheck + 18 测试 + lint 绿。**沉淀:全局 keydown 捕获(capture phase + preventDefault 拦截全部按键)极易制造键盘陷阱 —— 凡「捕获所有键」的 handler 必须显式放行焦点导航键(Tab/Shift+Tab)和关闭键(Escape),否则弹窗/捕获区成为键盘用户进得去出不来的死角。捕获组合 vs 焦点导航是冲突的:Tab 永远优先给焦点导航(bare Tab 几乎不作快捷键),不为「能绑 Tab」牺牲可达性。**
- **A63 修(P1,A62 同族:KeybindingCaptureModal 拦截 Enter/Space 致按钮无法激活)**:`KeybindingCaptureModal.tsx:100` A62 放行 Tab 后键盘用户能聚焦 取消/重置/保存,但 document capture handler 仍把 Enter/Space 当组合捕获并 preventDefault → 即使焦点在按钮上也无法激活,键盘断点仍在(进得去按不动)。→ 当事件目标位于原生交互控件(`button, a[href], input, select, textarea, [role=button]`)内且是**无 ctrl/meta/alt 修饰**的激活键(Enter / Space)时提前 return,放行控件激活;带修饰的 mod+Enter 等(按钮不会激活)仍按组合捕获,绑定能力不受损。+TDD(焦点在 Save 按钮按 Enter / Space:defaultPrevented=false 且 captured 未被改写;mod+Enter 仍捕获为组合 Save 启用)。typecheck + 21 测试 + lint 绿。**沉淀:键盘陷阱修复是「分层」的 —— A62 让 Tab 能进出焦点只是第一层,Enter/Space 激活键被全局捕获吞掉是第二层(焦点能到但按不动)。凡全局/捕获阶段拦截全部按键的 handler,放行清单要完整:焦点导航键(Tab)+ 关闭键(Esc)+ 焦点落在交互控件上时的激活键(Enter/Space)。判定「目标是否交互控件」用 e.target.closest(交互选择器);区分激活 vs 绑定:无修饰激活键给控件,带修饰仍可捕获为组合。**
- **A64 修(P1,全局热键在可编辑控件内劫持单键打字)**:`useCommandHotkeys.ts:95` 全局 keydown 监听对所有事件不论 target 都匹配已注册 hotkey,匹配即 preventDefault+执行。快捷键系统允许单键绑定(eventToCombo 对裸 'x' 返回 'x'),用户在 input/textarea/contenteditable 里键入该字符会被全局命令劫持 → 文本打不进去、焦点上下文被破坏。→ 抽 `isEditableTarget(target)`(INPUT/TEXTAREA/SELECT/isContentEditable),handler 中事件来自可编辑控件时跳过**无 ctrl/meta/alt 修饰**的绑定(`continue` 不 preventDefault,放行打字);带 ctrl/meta/alt 的全局组合(mod+s 保存等)在编辑器内仍生效。+TDD(input 内裸 'x'/shift+x → 不执行不阻止默认;input 内 mod+s → 仍执行;document 上裸 'x' → 仍执行)。typecheck + 25 测试 + lint 绿。**沉淀:键盘可达性族从「捕获 modal 自身陷阱」(A62/A63)延伸到「全局热键侵入文本编辑」—— 凡 document 级 keydown 命令分发都须按 target 上下文门控:可编辑控件内只放行带 ctrl/meta/alt 的组合,无修饰单键(及 shift-only)留给文本输入。判定用 isEditableTarget(tag + isContentEditable);区分「全局命令」与「文本输入」的界线是修饰键存在性,不是键本身。**
- **A65 修(P2,A56 同族:Quick Open 5000 文件截断提示不在 live region)**:`QuickOpenModal.tsx:274` 扫描到 5000 文件截断时动态显示的 limit_hint(「仅显示前 5000 个文件…」)在普通 `<div>` 无 role/aria-live → 焦点锁搜索框时 SR 用户只知有结果列表,不会获知结果被截断、搜索范围不完整。→ 给该提示容器加 `role="status"`(polite 中性信息提示)。+TDD(walk 返回 5000 文件 → 列表渲染后唯一 role=status 含「仅显示前 5000 个文件」)。typecheck + 16 测试 + lint 绿。**沉淀:live region 族延伸到「结果截断/不完整」提示 —— 不只「无结果空态」(A56/A57)和「结果计数」(A53-A55),「结果被截断」这类影响用户对搜索完整性认知的动态提示同样须 role=status;凡焦点锁输入框、信息在别处动态出现的场景一律补 live region。filtered>0 时 limit_hint 是页面唯一 role=status,可直接 querySelector 断言。**
- **A66 修(P2,A41 同族:Marketplace 索引加载失败块不在 live region)**:`MarketplaceTab.tsx:270` 扩展市场索引异步加载失败时渲染的错误块(`state.kind === 'error'`)是普通 `<div>` 无 role/aria-live → 焦点停在面板/搜索入口附近时 SR 用户只感知"无可用内容",不知是加载失败。→ 给错误块加 `role="alert"`(失败=assertive)。+TDD(fetchIndex reject → 错误块在 role=alert 且含「拉取索引失败」)。typecheck + 28 测试 + lint 绿。**沉淀:live region 族延伸到「整块内容区加载失败」—— 不只局部 toast/inline error(A41-A50),整个面板因索引/数据源加载失败而呈现错误态时也须 role=alert,否则与"空内容"无法区分。凡 `state.kind==='error'` 这类顶层失败渲染分支都查 live region。**
- **A67 修(P2,A56/A66 同族:Marketplace 索引为空空态不在 live region)**:`MarketplaceTab.tsx:284` 索引异步加载成功但为空时空态容器(「暂无插件」+ 仓库链接)无 role/aria-live → 焦点停在原控件时 SR 用户不会获知"市场为空"这个加载结果。→ 加 `role="status"`(polite 中性空态)。失败态用 alert(A66)、空态用 status(A67)严重度对应。+TDD(fetchIndex 返回空 → 空态在 role=status 含「暂无插件」)。typecheck + 29 测试 + lint 绿。**沉淀:整块内容区的异步加载三态(loading/error/empty)live region 收口 —— error→alert(A66)、empty→status(A67),loading 已有 Spinner aria-label;顶层 state machine 的每个终态分支都要可被 AT 感知,空与失败用不同 role 区分严重度。codex 自动 context 压缩后(85% 恢复)继续审计,无需重启会话。**
- **A68 修(P2,A41 同族:插件行运行时 error/warning 不在 live region)**:`PluginsTabContent.tsx:431/442` 插件行内 `p.error`(激活失败等)/`p.warning`(部分授权等)异步更新时只是普通文本无 role → 焦点在按钮/设置页别处时 SR 用户不被告知插件状态变坏。→ p.error 加 `role="alert"`(失败=assertive)、p.warning 加 `role="status"`(可覆盖警告=polite)。+TDD(error 在 role=alert 含 EBOOM;warning 在 role=status 含「部分授权」用 .some() 定位)。typecheck + 30 测试 + lint 绿。**沉淀:live region 族延伸到「列表行内的 per-item 运行时状态」—— 不只面板级三态(A66/A67),列表每行的异步 error/warning 状态字段也须按严重度上 live region(error→alert / warning→status)。这与 A42-A44 的安装结果反馈是同一文件的不同状态出口,凡渲染异步状态字段的分支都查。**
- **A69 修(P2,A56/A65 同族:Quick Open 无 workspace 空态不在 live region)**:`QuickOpenModal.tsx:206` 无 workspace 时显示 quick_open.no_workspace 空态但无 role/aria-live → 弹窗打开焦点自动落搜索框,SR 用户不被告知"当前无法搜索文件"及原因。→ 加 `role="status"`(polite 信息态)。+TDD(root=null + 打开 → 无 workspace 文案在 role=status)。typecheck + 17 测试 + lint 绿。**沉淀:QuickOpen 的全部呈现态 live region 收口 —— 无 workspace(A69)/扫描失败(A45 alert)/空与无匹配(A56 status)/5000 截断(A65 status);一个组件内 root 三态(无 root / scanning / 有结果)的每个非列表态都须可被 AT 感知。弹窗自动聚焦输入框 = 任何同弹窗的状态文本都在焦点之外,必须 live region。**
- **A70 修(P2,装饰符号族:Reviews 展开按钮三角未 aria-hidden)**:`MarketplaceTab.tsx:763` Reviews 展开按钮已用 aria-expanded 暴露状态,但视觉三角 ▴/▾ span 无 aria-hidden → SR 把三角混进按钮可访问名,造成"评分/评论数/三角/expanded"重复噪声。→ 三角 span 加 `aria-hidden="true"`(状态已由 aria-expanded 表达,三角纯装饰)。+TDD(折叠态 ▾ / 展开态 ▴ 三角 span 均 aria-hidden=true 且 aria-expanded 仍切换)。typecheck + 30 测试 + lint 绿。**沉淀:装饰符号族 —— 当语义已由正式 ARIA 属性(aria-expanded/aria-pressed/aria-selected)表达时,伴随的视觉指示符号(三角/箭头/勾)必须 aria-hidden,否则与 ARIA 状态双重表达成噪声;凡"按钮里既有 aria-* 状态又有状态符号"都查符号是否 hidden。A40 是把语义符号分离出可读文本,A70 是反向:状态已有正式属性则符号纯装饰须 hidden。**
- **A71 修(P2,A70 同族装饰符号:Explorer tree row 展开箭头未 aria-hidden)**:`FileRow.tsx:145` 目录行展开箭头 ▾/▸ span 无 aria-hidden(同行的 icon span 已有)→ treeitem 展开态由 headless-tree row props 的 aria-expanded 表达,箭头是纯视觉装饰,却混进行可访问名,SR 读文件名时夹杂三角噪声。→ 箭头 span 加 `aria-hidden="true"`。+TDD(折叠目录 ▸ / 展开目录 ▾ 箭头 span 均 aria-hidden=true)。typecheck + 6 测试 + lint 绿。**沉淀:装饰符号族(A70 Reviews 展开按钮 / A71 Explorer tree row)同模式 —— 凡「展开态已由 aria-expanded(库或自写)表达」的三角/箭头都须 aria-hidden;此处同一行已有 icon span aria-hidden 在先,箭头是漏网兄弟(修一族必查同元素内的所有装饰符号是否都 hidden)。**
- **A72 修(P2,A70 同族装饰符号:卡片仓库外链 ↗ 进可访问名)**:`MarketplaceTab.tsx:688` 卡片仓库外链文本 `{entry.repo} ↗` 把外链箭头 ↗ 直接放进链接可访问名 → SR 读成"repo arrow"噪声;链接目的已由仓库名表达,↗ 纯视觉提示。→ `↗` 包进 `<span aria-hidden="true">`。+TDD(仓库链接含 me/a 文本,↗ 在 aria-hidden span 内)。typecheck + 31 测试 + lint 绿。**沉淀:装饰符号族(A70 三角/A71 箭头/A72 外链 ↗)—— 外链 ↗ 是 icon-only-link(A36)的孪生反面:A36 是链接只有 ↗ 须补 aria-label,A72 是链接已有文本则 ↗ 须 aria-hidden;凡链接文本里夹纯视觉符号(↗/→/外链图标)都查是否 hidden。**
- **A73 修(P2,A72 同族装饰符号:verified 徽章 ✓ 未 aria-hidden)**:`MarketplaceTab.tsx:590` verified 徽章 `✓ {t('marketplace.verified')}` 的勾 ✓ 无 aria-hidden → "已验证"文本已表义,SR 额外读出 checkmark 造成静态徽标名称噪声。→ ✓ 包进 `<span aria-hidden="true">`。+TDD(verified 徽章渲染后 ✓ 在 aria-hidden span 内)。typecheck + 34 测试 + lint 绿。**沉淀:装饰符号族第四例(A70 三角/A71 箭头/A72 外链 ↗/A73 勾 ✓)—— 凡"图标符号 + 已表义文本"并列的徽标/标签,符号都须 aria-hidden;codex 在 MarketplaceTab 单文件内逐符号扫(▴▾/↗/✓)是"修一族必查兄弟"的彻底化。**
- **A74 修(P2,A2 同族:Agent revoke 按钮可访问名缺动作语义)**:`StatusBar.tsx:190` agent session revoke 按钮可见文本只有「N 个 agent」(● 已 aria-hidden),撤销/终止动作语义仅在 title → SR 聚焦只听到"N 个 agent button",不知按下会终止 agent terminal 并撤销授权(安全敏感操作)。→ 加 `aria-label`(复用含撤销语义的 `statusbar.mcp.revoke_tooltip`)。+TDD(revoke 按钮 aria-label 含「撤销」)。typecheck + 24 测试 + lint 绿。**沉淀:可访问名族(A1/A2)—— 当按钮可见文本只描述「对象/计数」而动作语义在 title 时,SR 有可见文本就不读 title,破坏性/安全动作(撤销授权)的语义必须进 aria-label;title 仅作鼠标 hover 提示不能替代可访问名。与装饰符号族(A70-A73)互补:符号噪声要去掉,动作语义要补上。**
- **A75 修(P2,A2 同族:插件行操作按钮可访问名不可区分)**:`PluginsTabContent.tsx:452-536` 每行 5 个操作按钮(重载/禁用/启用/重试/权限/卸载)可见文本通用,多插件行 SR 导航时全读成同样的"卸载/禁用",无法知道操作哪个插件(误操作风险,卸载尤甚)。→ 新增组合 key `plugins_tab.action.row_button_aria`='{action} — {name}'(三语言),每个行按钮加 `aria-label`=该 key(action=各自 btn 文案,name=p.manifest.name)。+TDD(两插件 alpha/beta 的卸载按钮 aria-label 各含各自插件名可区分)。typecheck(三语言 key 全等强制)+ 31 测试 + lint 绿。**沉淀:可访问名族延伸到「列表重复控件」—— 同一动作在多行重复时,通用可见文本不足以区分目标,须 aria-label 注入行实体名(插件名/文件名);用一个 '{action} — {name}' 组合 key 复用既有动作文案,避免每动作单独建 key。设计层 Button 透传 aria-label(extends ButtonHTMLAttributes + {...rest})使调用点注入可行。**
- **A76 修(P2,A75 同族:Keybindings 行编辑/重置按钮可访问名不可区分)**:`KeybindingsTabContent.tsx:212/222` 每行编辑(铅笔)/重置(回退)IconButton 的 aria-label 通用('编辑快捷键'/'恢复默认'),多命令行 SR 导航全读同样名,无法知道操作哪个命令(误操作)。→ 新增 `keybindings.edit_hotkey_for_aria`='编辑 {command} 的快捷键' / `keybindings.reset_default_for_aria`='将 {command} 的快捷键恢复默认'(三语言),aria-label 注入 d.displayTitle。+TDD(两命令 Save/Open 编辑按钮 aria-label 各含命令名)。**钉死旧通用 aria-label 的 3 个既有 override 测试 + 1 个编辑 Modal 测试** querySelector 从精确 `[aria-label=恢复默认/编辑快捷键]` 改子串 `[aria-label*="恢复默认"/"编辑"]`(旧文案已不存在)。typecheck + 17 测试 + lint + i18n 回归绿。**沉淀:可访问名族「列表重复控件注入实体名」第二例(A75 插件行 / A76 快捷键行)—— 同模式跨面板复发,修一族必查所有列表型设置面板;改控件 aria-label 必同步改钉死旧 aria-label 的既有测试选择器(精确→子串),与 i18n 重命名同源的测试维护点。**
- **A77 修(P2,A75/A76 同族第三例:Marketplace 卡片操作按钮可访问名不可区分)**:`MarketplaceTab.tsx:602/615/630` 卡片安装/更新/已装按钮可见文本通用(「安装」「更新到 X」「已安装」),多卡片 SR 导航全读同样名,无法知道操作哪个插件(误操作)。→ 新增 `marketplace.card_action_aria`='{action} — {name}'(三语言),三个卡片按钮 aria-label 注入 entry.name + 各自动作文案。+TDD(两卡片 Foo/Bar 安装按钮 aria-label 各含名)。typecheck + 32 测试 + lint 绿。**沉淀:可访问名族「列表重复控件注入实体名」第三例(A75 插件行 / A76 快捷键行 / A77 市场卡片)—— 同一缺陷模式在三个不同列表面板复发,codex 逐面板报全;一个 '{action} — {name}' 组合 key 通吃多动作(install/update/installed),既有 textContent 定位的测试不受影响(只加 aria-label 不改可见文本)。**
- **A78 修(P2,A75 同族第四例 + 设计层扩展:recent 目录菜单项同名不可区分)**:`ExplorerHeader.tsx:213` "打开最近"菜单项可见文本只有目录 basename,完整路径只在 title → 两个同 basename 不同 parent 的最近目录,SR 在菜单里只听到重复同名项,无法判断切到哪个路径。→ **design 层 MenuItem 加 `ariaLabel?` 可选 prop**(Continuo-local 微调,文件顶注释说明;设计层不引 i18n,文本由调用点注入),recent 项传 `t('panels.explorer.menu.open_recent_item_aria', {path:p})`(新增三语言='打开最近:{path}')。+TDD(同 basename 的 /a/work 与 /b/work recent 项 aria-label 各含完整路径可区分)。typecheck + 18 测试 + lint + design-layer-no-keys 守卫绿。**沉淀:可访问名族第四例(列表重复控件)延伸到「菜单项」—— 截断/同名的可见文本须 aria-label 注入可区分信息;design 层组件缺 aria-label 透传时,按 Continuo-local 约定加 string prop(非 i18n)由调用点注入本地化文本,与 i18n 禁令不冲突(A56 i18n 沉淀「设计层禁 i18n 须 app 调用点注入」同源)。**
- **A79 修(P2,A77 同族:Marketplace 卡片 review 链接可访问名不可区分)**:`MarketplaceTab.tsx:749/791` 每卡片 review 链接「写第一条评论」/「写评论」可见文本通用,多卡片 SR 逐项导航无法知道给哪个插件写(点错)。→ 两处链接 aria-label 复用 A77 的 `marketplace.card_action_aria`='{action} — {name}' 注入 entry.name(无新 key)。+TDD(两卡片 Foo/Bar review 链接 aria-label 各含名)。typecheck + 33 测试 + lint 绿。**沉淀:可访问名族「列表重复控件」第五例(同一 MarketplaceTab 内卡片按钮 A77 + review 链接 A79 两类重复控件);组合 key '{action} — {name}' 一次建立后可跨按钮/链接/任意动作复用,新增同族修复零新 key。codex 在收尾阶段沿单一族(同名不可区分)逐呈现点清点,是收敛前的彻底化扫尾。**
- **A80 修(P2,A78 同族:EmptyWorkspace 最近目录按钮同名不可区分)**:`EmptyWorkspace.tsx:52` 空 workspace 的最近目录列表按钮可见名由 basename + parent 组成(parent 可截断),完整路径只在 title → 同名/截断时 SR 无法区分打开哪个目录。→ 复用 A78 的 `panels.explorer.menu.open_recent_item_aria({path})` 给按钮加 aria-label(Button 透传)。+TDD(同 basename 的 /a/work 与 /b/work 按钮 aria-label 各含完整路径)。typecheck + 6 测试 + lint 绿。**沉淀:recent-目录可访问名族两处呈现点收口(A78 ExplorerHeader 菜单项 / A80 EmptyWorkspace 列表按钮)—— 同一"最近目录"数据在两个 UI 入口都需完整路径 aria-label;A78 建的 key 在 A80 复用,组合/注入式 aria key 是跨入口零成本复用的资产。可访问名族至此六例(A75-A80)。**
- **A81 修(P2,A79 同族:Reviews「See all in GitHub」链接多卡片同名)**:`MarketplaceTab.tsx:839` reviews 展开区(>10 条评论时)的 "See all in GitHub" 链接每卡片可访问名相同 → SR 在链接列表/多卡片展开时不知查看哪个插件的全部 reviews。→ aria-label 复用 `card_action_aria` 注入 entry.name(零新 key)。+TDD(11 条评论展开后 see-all 链接 aria-label 含插件名 Foo 且 href 含 discussions)。typecheck + 34 测试 + lint 绿。**沉淀:可访问名族第七例,MarketplaceTab 单文件内重复控件三类收口(卡片按钮 A77 + review 链接 A79 + see-all 链接 A81);组合 key 复用使每例零新 key。codex 收尾阶段沿"同名不可区分"族把同一文件的每个重复控件呈现点逐一报全=单族穷尽。**
- **A82 修(P2,A74 同族:MCP copy 按钮可访问名随状态漂移)**:`StatusBar.tsx:176` MCP 复制按钮可见文本 mcpLabel 随复制状态变化(idle/已复制/失败),动作语义只在 title → SR 聚焦时可能只读到短状态文案,且按钮可访问名随结果漂移。→ 加稳定 `aria-label={t('statusbar.mcp.tooltip')}`(动作语义「复制 MCP 配置」),复制结果由 A51 的 live region 播报,按钮名不漂移。+TDD(点击后可见文本变「已复制」但 aria-label 不变)。typecheck + 25 测试 + lint 绿。**沉淀:可访问名族「按钮名随状态漂移」—— 当按钮可见文本会因操作结果变化(状态机文案)时,必须给稳定 aria-label 锁住「动作语义」,结果交给 live region(A51);动作语义按钮(A74 撤销/A82 复制)的可访问名永远表"按下做什么"而非"当前状态/结果"。**
- **A83 修(P2,A36/A79 同族:ReviewItem GitHub 外链多条同名)**:`MarketplaceTab.tsx:924` 单条 review 的 GitHub 图标外链 A36 已补 aria-label,但每条 review 都是同一个「在 GitHub 打开」→ 多条评论展开后 SR 逐项导航无法区分打开哪条。→ 新增 `marketplace.reviews.open_in_github_by`='在 GitHub 打开 {author} 的评论'(三语言),aria-label 注入 r.author.handle。+TDD(两条 review alice/bob 的链接 aria-label 各含作者)。typecheck + 35 测试 + lint 绿。**沉淀:可访问名族「列表重复控件」最深一层 —— 即使已有 aria-label(A36)仍可能"有名但不可区分";同质列表项(reviews)的链接须注入项级上下文(作者/日期),不只动作语义。A36(补名)→A83(注入上下文)是同一控件的两层修复:先有可访问名,再让名在同质列表中唯一。**
- **A84 修(P2,A73 同族装饰符号在 live region):插件 warning ⚠ 未 aria-hidden**:`PluginsTabContent.tsx:447` A68 给 p.warning 加了 role=status,但 live region 文本直接含 ⚠ 字符 → SR 把装饰警告符号混进状态播报成「warning sign + 部分授权…」噪声(严重度已由 role/文本表达)。→ ⚠ 包进 `<span aria-hidden="true">`。+TDD(warning live region 内 ⚠ 在 aria-hidden span,警告文本在外)。typecheck + 31 测试 + lint 绿。**沉淀:装饰符号族 × live region 交集 —— live region 里的状态符号(⚠/✘)尤其有害,因为它会被主动播报进状态流;凡 role=status/alert 容器内的前导符号都须 aria-hidden,严重度交给 role + 文本。这是 A68(加 live region)埋下、A84(清符号)收口的同控件两层(同 A36→A83 模式)。**
- **A85 修(P2,A84 同族:✘ 拼进 alert message 字符串)**:`MarketplaceTab.tsx:166`(卡片安装 catch)+`:446`(Git URL 安装 catch)把装饰性 ✘ 硬拼进 message 字符串,经 role=alert live region 被当内容播报成"cross mark …"噪声(失败严重度已由 isError+role=alert 表达)。→ 两处 catch 移除 ✘ 前缀(只存 errorMessage(err))。+TDD(两 catch 路径 alert 文本含 err 但 not.toContain('✘'))。typecheck + 35 测试 + lint 绿。**沉淀:装饰符号入 live region 有两种来源 —— ① 渲染时前导符号(A84 ⚠ 在 JSX)用 aria-hidden span;② 符号被拼进 message 数据字符串(A85 ✘)则源头不拼(数据层不该含装饰符号)。注:catalog 字符串(install_failed_code='✘ ...')里的 ✘ 是第三处同类(下一候选),本轮按"一次一个"只修 catch 路径;既有 catch 测试只断言 err 内容不钉 ✘ 故未破。**
- **A86 修(P2,A85 同族收口:catalog 失败文案内置 ✘)**:`electron/shared/i18n-locales/{en,zh,ko}.ts` 6 个失败文案 key(`plugins_tab.error.generic`/`plugins_tab.install.uninstall_fail`/`marketplace.install_failed_code`/`update_failed_code`/`update_failed_msg`/`index_fetch_failed`)内置装饰性 ✘ → ok=false 路径把翻译直接放进 role=alert,SR 播报 cross mark 噪声(A85 只修了 catch 路径,这些是 !r.ok 路径的源头)。→ 三语言 18 处统一移除 `'✘ '` 前缀(失败语义由 isError+role=alert 表达)。两处既有测试标题去 ✘(断言本就只查 message 子串不查 ✘,未破)。typecheck + 全套(marketplace/plugins/i18n 98 测试)+ lint 绿。**沉淀:装饰符号入 live region 三来源全收口 —— ①JSX 前导符号 aria-hidden(A84)②组件拼 message 字符串源头不拼(A85)③catalog 文案内置符号源头去除(A86);数据/文案层(catalog)不该含纯装饰符号,因为它必被某处当内容渲染/播报。修一族必查三来源:JSX、组件字符串、catalog。**
- **A87 修(P2,A86 同族成功侧:catalog 成功文案内置 ✔)**:`electron/shared/i18n-locales/{en,zh,ko}.ts` 3 个成功文案 key(`plugins_tab.install.success`/`marketplace.install_success`/`marketplace.update_success`)内置装饰性 ✔ → 安装/更新成功进入 role=status live region,SR 播报 check mark 噪声(成功语义已由 status + 文本表达)。→ 三语言 9 处移除 `'✔ '` 前缀。+TDD(安装成功 status 文本含「已安装 A」但 not.toContain('✔'))。typecheck + 全套 + lint 绿。**沉淀:catalog 装饰符号失败侧(✘ A86)+ 成功侧(✔ A87)对称收口 —— live region 文案(无论 alert 失败还是 status 成功)都不该含状态符号,严重度/结果由 role + 纯文本表达;符号是视觉增强,只能在渲染层以 aria-hidden 出现,绝不入数据/catalog。装饰符号族全谱收口:三角/箭头/勾(A70-A73)+ live region 三来源 ×(✘/✔)(A84-A87)。**
- **A88 修(P2,A79/A81 后果 + A86 同族:review action 文案内置 ✏️/↗ 被复用进 aria-label)**:`marketplace.reviews.{write_first,write_review,see_all_in_github}` catalog 文案内置 ✏️/↗ 视觉符号,A79/A81 把这些文案复用进 aria-label → 可访问名变成"pencil/arrow + 写评论 — 插件名"装饰噪声。→ 三语言剥离符号(write_first/write_review 去 ✏️ 前缀+↗ 后缀,see_all 去 ↗ 后缀,共 9 处),JSX 3 处链接用 `<span aria-hidden="true">` 单独渲染 ✏️/↗ 保视觉。+TDD(review 链接 aria-label 含插件名但 not.toContain ✏️/↗)。typecheck + 35 测试 + lint 绿。**沉淀:复用「可见文本 catalog」拼 aria-label 的隐患 —— 若该文案含装饰符号(✏️/↗),符号会漏进可访问名(A79/A81 埋,A88 收口);根因解=可见文本 catalog 也不该含装饰符号(同 A86/A87 原则),符号全部移到 JSX aria-hidden。这是装饰符号族的最终统一:无论 catalog 用于显示、alert、status 还是 aria-label,都不含符号;符号只活在 JSX 的 aria-hidden span。**
- **A89 修(P2,A84/A88 同族:keybindings conflict_title catalog 内置 ⚠️)**:`keybindings.modal.conflict_title` catalog 文案内置 ⚠️,渲染进 A52 的 role=status 冲突警告 live region → 捕获快捷键后 SR 播报装饰 warning emoji(严重度已由警告样式/文本表达)。→ 三语言剥离 ⚠️,JSX(KeybindingCaptureModal conflict 标题)用 `<span aria-hidden="true">⚠️</span>` 单独渲染。+TDD(冲突警告内 ⚠️ 在 aria-hidden span)。typecheck + 21 测试 + lint 绿。**沉淀:catalog 装饰符号清理跨文件复发(marketplace ✘✔✏️↗ → keybindings ⚠️);凡 catalog 文案首/尾的状态/装饰符号都须移到 JSX aria-hidden。剩 `marketplace.reviews.new_account_badge`(⚠ 静态 badge 非 live region)为同类下一候选,已告知 codex。**
- **A90 修(P2,A88 同族:刷新评分按钮 ⟳ 进可访问名)**:`marketplace.reviews.refresh`/`refreshing` catalog 内置 ⟳,刷新按钮直接用作可见文本且无 aria-label → 按钮可访问名含装饰刷新符号。→ 三语言剥离 ⟳(refresh/refreshing 共 6 处),JSX 按钮用 `<span aria-hidden="true">⟳</span>` 单独渲染图标。+TDD(刷新按钮 ⟳ 在 aria-hidden span,文本节点含「刷新评分」无符号)。typecheck + 36 测试 + lint 绿。**沉淀:catalog 装饰符号清理已扫 ✘✔✏️↗⚠️⟳ 六种符号;凡「按钮/链接文本 = catalog 文案 + 无独立 aria-label」且 catalog 含符号,符号必进可访问名 → 根因移符号出 catalog + JSX aria-hidden。剩 new_account_badge(⚠)候选未修。**
- **A91 修(P2,A40+A89 复合:new-account badge ⚠ + 风险说明只在 title)**:`MarketplaceTab.tsx:907` 新账号警告 badge 两个缺陷:① 可见文本含装饰 ⚠(catalog `new_account_badge`);② 关键风险说明 `new_account_warn`(「GitHub 账号注册不到 N 天,慎信」)只放在非交互 span 的 title → 键盘用户无法 hover、SR 读 title 不可靠。→ ⚠ 从 catalog 剥离(3 语言)+ JSX aria-hidden 渲染;new_account_warn 改用视觉隐藏 SR_ONLY span 让 AT 可读(保留 title 供鼠标)。+TDD(badge 含 ⚠ 的 aria-hidden span + 风险说明天数文本在 DOM 可读)。typecheck + 37 测试 + lint 绿。**沉淀:catalog 全部 7 种装饰符号(✘✔✏️↗⚠️⟳⚠)清理完毕——全仓 catalog 已无任何装饰符号。A91 是装饰符号族(A70-A91)收尾,且复合了可达名族「title-only 信息不可达」(A40 同源)——非交互元素的 title 对键盘/SR 都不可靠,关键信息须进视觉隐藏文本或 aria。**
- **A92 修(P2,A88 同族:maintainer badge 🛡 在 catalog)**:`marketplace.reviews.maintainer_badge` catalog 内置装饰 🛡(第 8 种符号,我此前的窄 grep 漏掉,codex 用更全符号集 `[✔✘⚠✓↗👍★☆⟳▴▾●✕✏🛡]` 扫出)→ reviewer 元信息朗读混入盾牌符号("维护者"文本已表义)。→ 三语言剥离 🛡,JSX 用 aria-hidden span 渲染。+TDD(maintainer badge 🛡 在 aria-hidden span,文本含「维护者」)。typecheck + 38 测试 + lint 绿。**用 codex 全符号集复扫:全仓 catalog 装饰符号 0。沉淀:自检符号清理时窄 grep 会漏(我只扫 7 种漏了 🛡),换审计者的更全符号集是查漏价值——「换审计者捞自审盲区」在符号枚举层面再次印证;清装饰符号必用最全 emoji/符号字符集(含盾牌/星/竖线等)兜底。**
- **A93 修(P2,A51 同族:reviews 刷新 loading 态不在 live region)**:`MarketplaceTab.tsx:352` 点刷新评分进入 loading 时只把按钮文字改「刷新中…」+ disabled,无独立 role=status/aria-busy → 焦点在按钮的 SR 用户听不到"正在刷新",只能等成功无声或失败 alert。→ 按钮加 `aria-busy={reviewsLoading}` + 旁置视觉隐藏 `role=status`(仅 loading 时输出「刷新中」,idle 空不打扰,复用 SR_ONLY_STYLE)。+TDD(loading 时按钮 aria-busy=true + role=status 含「刷新中」)。typecheck + 39 测试 + lint 绿。**沉淀:瞬时 loading 态 live region(A93)与瞬时结果态(A51 复制结果)同模式 —— 按钮文字变化+disabled 不足以让焦点在按钮的 AT 感知,须 aria-busy(忙碌语义)+ sr-only role=status(播报);async 操作三态 loading/success/error 都要 AT 可感:loading→status+aria-busy、success→status、error→alert。**
- **A94 修(P2,A93 同族:install/update loading 态不在 live region)**:`MarketplaceTab.tsx:488`(Git URL 安装)+ `:623`(卡片更新)+ `:663`(卡片安装)安装/更新进入 loading 仅改按钮文字(安装中…/更新中…)+ disabled,无 aria-busy/status。→ 三按钮加 `aria-busy={busy/installing}` + 各区(Git URL section / 卡片)旁置视觉隐藏 `role=status` 镜像「安装中」(仅 loading 时输出)。+TDD(卡片安装点击后 aria-busy=true 按钮存在 + role=status 含「安装中」)。typecheck + 40 测试 + lint 绊。**沉淀:loading 态 live region 收口三处(A93 reviews 刷新 + A94 Git URL/卡片安装/更新)—— 凡「点击后按钮变文字+disabled 表示进行中」都须 aria-busy + sr-only status;async 三态(loading/success/error)在每个操作入口都要齐(success/error 此前 A42-A44 已补,A93/A94 补齐 loading)。**
- **A95 修(P2,A94 兄弟入口:Plugins 页 Git URL 安装 loading 不在 live region)**:`PluginsTabContent.tsx:362` Plugins 管理页 Git URL 安装 loading 仅改按钮文本+disabled,无 aria-busy/status(Marketplace A94 的跨面板兄弟)。→ 导入 SR_ONLY_STYLE,安装按钮加 `aria-busy={installing}` + 旁置视觉隐藏 `role=status` 播报「安装中」。+TDD(pending promise 触发 loading → 按钮 aria-busy=true + role=status 含「安装中」);**钉死单数 querySelector('[role=status]') 的既有 A42 成功测试改 .some()**(新增 loading role=status idle 空,DOM 靠前致单数选择器命中空 span,同 A52/A61)。typecheck + 72 测试 + lint 绿。**沉淀:loading 态 live region 跨面板兄弟(A93/A94 Marketplace + A95 Plugins)—— 同一安装 loading 模式在两面板复发,修一族必查兄弟面板;新增 role=status 必排查同页钉死单数 [role=status] 选择器的既有测试(成功+loading 两 status 共存→.some()),这是 live region 修复的固有连带测试维护点。**
- **A96 修(P2,A93-95 兄弟:EmptyWorkspace Open Folder loading 不在 live region)**:`EmptyWorkspace.tsx:31` 打开系统目录选择器时仅按钮文字变「打开中…」+ disabled,无 aria-busy/status;原生对话框切换期间焦点反馈尤其不稳。→ 导入 SR_ONLY_STYLE,按钮加 `aria-busy={busy}` + 旁置视觉隐藏 `role=status` 播报「打开中」。+TDD(pending selectDirectory → 按钮 aria-busy=true + role=status 含「打开中」)。typecheck + 7 测试 + lint 绿。**沉淀:loading 态 live region 第四处(A93 reviews/A94 Marketplace 安装/A95 Plugins 安装/A96 EmptyWorkspace 打开目录)—— 凡用户触发的异步操作(网络/IPC/原生 picker)按钮进入 disabled+文字变化态都须 aria-busy + sr-only status;async 三态(loading/success/error)是 a11y 反馈的完整契约,任一态缺失 AT 用户都会陷入"点了没反应"的不确定。**
- **A97 修(P2,结构分组:Reviews 排序按钮组缺 role=group)**:`MarketplaceTab.tsx:844` 排序 toggle 组有可见「Sort:」标签 + 两个 aria-pressed toggle,但无 role=group/aria-labelledby → SR 逐个聚焦只听到按钮状态,不知这是 review 排序控件。→ 排序行加 `role="group"` + `aria-labelledby` 指向带 id 的 sort 标签(id 含 entry.id 保多卡片唯一)。+TDD(role=group 存在 + aria-labelledby 指向非空标签 + 组内 2 个 aria-pressed 按钮)。typecheck + 40 测试 + lint 绿。**沉淀:相关 toggle 控件成组须 role=group + aria-labelledby 关联组名(A16/A8 补了单按钮 aria-pressed,A97 补组语义)—— 单控件 ARIA 状态(pressed/expanded)+ 组容器语义(group + 组名)是两层;jsdom 无 CSS.escape,按 id 查元素用属性选择器 `[id="..."]`(同 A21)。**
- **A98 修(P2,A97 同族:Popular tags 筛选组缺 role=group)**:`MarketplaceTab.tsx:319` tag 筛选 toggle 组有多个 aria-pressed toggle 但容器无 role=group/aria-labelledby → SR 逐个聚焦 tag 只听到 tag 名+pressed,不知是筛选条件组。→ tag 容器加 `role="group"` + `aria-labelledby` 指向「Popular tags」标签(顶层单实例,静态 id)。+TDD(role=group + aria-labelledby 指向非空标签 + 组内 aria-pressed 按钮)。typecheck + 41 测试 + lint 绿。**沉淀:结构分组族(A97 排序组 / A98 tag 筛选组)—— 凡 aria-pressed/toggle 控件成组且有可见组标签,都须 role=group + aria-labelledby;codex 沿「成组控件组名」方向逐组扫(排序→tag)。单卡片实例用 entry.id 拼 id,顶层单实例用静态 id。**
- **A99 修(P2,A97 同族:Terminal 搜索选项 toggle 组缺组名)**:`TerminalSearchBar.tsx:94` regex/case/whole-word 三 aria-pressed toggle 无成组语义/组名 → SR 逐个聚焦只听到单开关状态,不知是「搜索选项」组。→ 三 toggle 用 `<div role="group" aria-label={t('panels.terminal.search.options_group')}>` 包住(新增组名 key 三语言;inline-flex gap-1 保视觉间距与外层一致)。+TDD(role=group 有可访问名 + 三 toggle 在组内 + 3 个 aria-pressed)。typecheck + 38 测试 + lint 绿。**沉淀:结构分组族三处收口(A97 排序/A98 tag 筛选/A99 终端搜索选项)—— 凡 toggle 控件成组都须 role=group;有可见组标签用 aria-labelledby(A97/A98),无可见标签用 aria-label 注入本地化组名(A99,设计层 IconButton 由调用点注入文本)。包裹组容器时用 inline-flex+相同 gap 保视觉(避免 display:contents 的 a11y 树兼容性坑)。**
- **A100 修(P2,combobox aria-expanded 语义错误)**:`CommandPalette.tsx:191` + `QuickOpenModal.tsx:191` modal combobox 设 `aria-expanded={filtered.length > 0}` → 无结果时宣告 collapsed,但弹层实际仍可见(显示「无匹配」空态),aria-expanded=false 与可见状态矛盾,SR 用户误以为列表已收起。→ 这类「打开即弹层常驻」的 modal combobox `aria-expanded` 恒 true;无结果只移除 aria-activedescendant(本已正确)。+TDD(两 modal 无结果时 aria-expanded='true' 且 aria-activedescendant=null)。typecheck + 56 测试 + lint 绿。**第 100 轮里程碑。沉淀:aria-expanded 表「受控弹层是否可见」而非「是否有结果」—— 常驻弹层(modal 内 combobox)恒 true,把它绑定到结果数是常见误解;aria-activedescendant 才随结果有无切换。CommandPalette/QuickOpen 是 combobox 孪生,凡一处改 combobox ARIA 必同步另一处(A15 起一直成对)。**
- **A101 修(P2,A100 后续:aria-controls 悬空)**:`CommandPalette.tsx:195` + `QuickOpenModal.tsx:194` combobox 始终设 `aria-controls="...-listbox"`,但 listbox `<ul id>` 仅在 filtered.length>0 时渲染 → 无结果时 aria-controls 指向不存在元素,combobox 关系断裂。→ 仅有结果时设 `aria-controls`(`filtered.length>0 ? id : undefined`)。+TDD(两 modal 无结果时 aria-controls=null;既有有结果测试仍断言 = listbox id)。typecheck + 56 测试 + lint 绿。**沉淀:combobox 三属性须协同随结果数 —— aria-expanded 恒 true(弹层可见,A100)、aria-controls 仅 listbox 存在时设(A101)、aria-activedescendant 仅有高亮项时设;A100 修 expanded 引出 A101 controls 悬空(同控件连带:改一个 ARIA 属性须核对引用的目标元素是否真渲染)。**
- **A102 修(P2,A93 同族:QuickOpen 扫描中态泛化 loading)**:`QuickOpenModal.tsx:214` 扫描中态只有 Spinner 自带泛化 `role=status`+`aria-label="Loading"`,旁边「正在扫描」文本不在 live region → SR 只听到泛化 loading,不知在扫描文件。→ 扫描容器加 `role="status"`(播报「扫描中…」具体语义)+ Spinner 加 `aria-hidden`(抑制其泛化 role=status,避免双 status / 只听 Loading)。+TDD(扫描态 role=status 含「扫描中」+ 内含 aria-hidden 元素)。typecheck + 18 测试 + lint 绿。**沉淀:design Spinner 自带 role=status+aria-label="Loading"(泛化)—— 当 loading 有具体语义(扫描/安装中)时,须给 Spinner aria-hidden 让具体文本的 live region 接管,否则 AT 只听泛化"Loading"或双重播报;loading 态分泛化(纯 Spinner 可)与具名(须容器 role=status + Spinner 隐藏)两类。**
- **A103 修(P2,A102 同族 + 双兄弟:Terminal 启动 overlay 泛化 loading)**:`TerminalView.tsx:44` + `TerminalPanelView.tsx:182` 终端启动 overlay 具体文本「启动 shell」aria-label 挂在无 role 的 div 上(不可靠),实际可播报的只有 Spinner 泛化 Loading。→ 两处同 A102 修法:启动文本容器加 `role="status"`、Spinner 加 `aria-hidden`、移除无 role div 的不可靠 aria-label。codex 明示同模式兄弟,一并修两文件。+TDD(TerminalView 启动态 role=status 含「启动 shell」+ 内含 aria-hidden;isReady=true 无该 status)。**钉死 `[aria-label="启动 shell"]` 的既有 terminal-view 测试 2 处改 role=status 定位**。typecheck + 12 测试 + lint 绿。**沉淀:具名 loading 态 live region 跨组件四处(A102 QuickOpen 扫描 + A103 TerminalView/TerminalPanelView 启动);「aria-label 挂无 role 元素」是反复出现的反模式(A40/A74/A91/A103)——状态/名称语义须挂在有语义角色的元素(role=status / 真控件)上才可靠。codex 明示的同模式兄弟应同轮一并修。**
- **A104 修(P2,A102/A103 同族:Marketplace 初始索引加载泛化)**:`MarketplaceTab.tsx:262` 初始索引加载态只渲染 Spinner 泛化 `aria-label="Loading"`,无具体「正在加载插件市场」语义。→ 新增 `marketplace.loading` 三语言,加载容器加 `role="status"` + 具体文本 + Spinner `aria-hidden`(升级 I16 的「Spinner 本地化 aria-label」为 role=status 容器播报具名文本)。+TDD(role=status 含「正在加载插件市场」+ Spinner aria-hidden;原 I16 spinner-aria-label 测试升级)。typecheck + 41 测试 + lint 绿。**沉淀:具名 loading live region 跨组件五处收口(A102 QuickOpen 扫描 / A103 Terminal×2 启动 / A104 Marketplace 索引);泛化 Spinner aria-label(I16 当年的修法)在「加载有具体对象」时仍不足——升级为 role=status 容器 + 具名文本 + Spinner aria-hidden。所有面板级初始加载态都应播报「正在加载<什么>」而非通用 Loading。**
- **A105 修(P2,i18n+a11y:dock tab 关闭按钮可访问名硬编码英文)**:`SharedTab.tsx:175` icon-only dock tab 关闭按钮 aria-label = 模板字面 `Close ${title}` → zh/ko 界面 SR 读英文「Close」,且这是该图标按钮唯一语义来源。→ 新增 `shell.tab.close`='Close {title}'/'关闭 {title}'/'{title} 닫기',aria-label 改 `t('shell.tab.close', { title })`。+TDD(en→'Close Hello'、zh→含「关闭」不含「Close」;**钉死英文 'Close' 的两个既有测试选择器改 zh「关闭」**,因测试默认 locale=zh)。typecheck + 9 测试 + lint 绿。**沉淀:硬编码英文 aria-label 是 i18n×a11y 交集漏网(可见文本走了 i18n 但 aria-label 用模板字面英文)——icon-only 按钮的 aria-label 是唯一语义来源,必须本地化;凡 `aria-label={`...${x}`}` 模板字面都要查是否该走 t()。改 aria-label 必同步钉死旧英文的既有测试(默认测试 locale=zh,旧测试钉英文实为测旧硬编码值)。**
- **A106 修(P2,A105 同族 design 层:editor tab 关闭按钮硬编码英文)**:`TabNav.tsx:99` design 层 TabNavItem 关闭按钮 aria-label 硬编码 `Close ${title}`/`Close tab` → zh/ko 下 icon-only 关闭按钮唯一可访问名是英文。design 层禁 i18n → 加可选 `closeLabel` prop(caller 注入本地化,同 dirtyLabel 模式),未提供回退英文默认;EditorHeader 调用点传 `t('shell.tab.close',{title:basename})`。+TDD(design 层:closeLabel 注入→用之 / 未提供→英文回退;EditorHeader:close 按钮 aria-label 本地化用子串定位;钉死英文 'Close /x.md' 的既有测试改子串)。typecheck + 32+12 测试 + lint + design-no-keys 守卫绿。**沉淀:icon-only 关闭按钮本地化两层 —— app 层组件(A105 SharedTab)直接 t();design 层组件(A106 TabNavItem)按铁律加 caller-injected prop(closeLabel,英文 fallback)由调用点注入,与 dirtyLabel(A35)同模式。design 层任何「唯一语义来源是英文字面」都走这条:加 prop + 英文 fallback + 调用点注入。**
- **A107 修(P2,A99 同族 design 层:tablist 缺组名)**:`TabNav.tsx:80` design 层 TabNav 声明 `role="tablist"` 但无 aria-label/labelledby,生产编辑器标签栏也未传组名 → SR 进入 tablist 只听泛化"tab list",不知是编辑器打开文件标签组。→ design TabNav 加 caller-injected `ariaLabel` prop(无 i18n 依赖),EditorHeader 传 `t('shell.tab.editor_tablist')`(新增三语言「编辑器标签」)。+TDD(design 层 ariaLabel prop→tablist aria-label;EditorHeader tablist 有本地化名)。typecheck + 45 测试 + lint + design-no-keys 守卫绿。**沉淀:结构分组族扩展到 tablist(A97/A98/A99 group + A107 tablist)—— 凡 role=tablist/group/radiogroup 等 composite 容器都须可访问名;design 层 composite 容器用 caller-injected ariaLabel prop(同 closeLabel/dirtyLabel),app 层调用点注入本地化组名。codex 提示 design Tabs 可能同需此能力(下一候选)。**
- **A108 修(P2,A107 同型 design 层:Tabs 组件无 tablist 组名能力)**:`Tabs.tsx:35` 导出的 design Tabs 组件 role="tablist" 无 aria-label/labelledby 能力 → 任何后续使用者都暴露无名 tablist。→ 同 TabNav(A107)加 caller-injected `ariaLabel` prop(无 i18n);**当前生产无调用点 → 不新增 catalog key**(纯组件契约补强,预防性)。+TDD(design 层:ariaLabel 注入→tablist aria-label;未提供→null)。typecheck + 5 测试 + lint 绊。**沉淀:导出 design 组件的契约缺口(无可访问名能力)即便当前无调用点也应补,避免后续使用者继承缺陷——这是「组件契约完整性」预防层,区别于「当前生产实际影响」修复(codex 明确区分二者,无调用点的契约补强不新增 i18n key)。结构分组族 design 层收口:TabNav(A107)+ Tabs(A108)两个导出 tablist 组件都有 ariaLabel 能力。**
- **A109 修(P2,i18n×a11y:FileRow 插件 badge 硬编码英文 aria-label)**:`FileRow.tsx:187` 插件装饰 badge 的 aria-label 硬编码 `badge ${decoration.badge}` → 位于 treeitem 行朗读路径,zh/ko 用户听到英文「badge」,且 aria-label 覆盖了原本可见 badge 文本。→ 去掉硬编码 aria-label,让可见 badge 文本自然朗读(badge 语义由插件决定,无通用本地化;插件如需可访问名可经 decoration 自带)。+TDD(badge 用 .tabular-nums 定位含可见值,无英文 aria-label;空/null decorator 无 badge span)。typecheck + 6 测试 + lint 绿。**沉淀:硬编码英文 aria-label 反模式第三例(A105 SharedTab close/A106 TabNav close/A109 FileRow badge)——aria-label 覆盖可访问名,若内容是「英文词 + 可见值」且无 i18n,不如去掉让可见文本自然朗读(覆盖式 aria-label 比无 aria-label 更糟,因它既英文又丢可见信息);凡 `aria-label={`<英文词> ${可见值}`}` 优先评估能否直接删除让可见文本读。**
- **A110 修(P2,A93/A102 同族:文件树行加载态无 aria-busy)**:`FileRow.tsx:193` 展开目录加载子项时行只显示裸 …,无 aria-busy/可读加载文本 → SR 用户只遇省略号噪声,不知该 treeitem 正在加载。→ 行根(getProps 后)加 `aria-busy={isLoading||undefined}`,视觉 … 标 `aria-hidden`,追加视觉隐藏「加载中」(t('common.loading'))。+TDD(isLoading→行根 aria-busy=true + … aria-hidden + 「加载中」文本可读)。typecheck + 7 测试 + lint 绿。**沉淀:loading 态 live region/语义六处收口(A93 reviews/A94 安装/A95 Plugins/A96 EmptyWorkspace/A102 QuickOpen/A103 Terminal/A104 Marketplace/A110 文件树行);treeitem 这类「行内瞬时加载」用 aria-busy(行根忙碌语义,treeitem 本身就是 AT 焦点)+ sr-only 文本,而非独立 role=status(行在树里,aria-busy 更贴语义)。loading 反馈按容器语义选:面板级 role=status,列表行级 aria-busy。**
- **A111 修(P2,A101 同族:aria-activedescendant 下标越界)**:`QuickOpenModal.tsx:196` + `CommandPalette.tsx:200` aria-activedescendant 只判 `filtered.length>0`,未校验 `selectedIndex < filtered.length` → 结果异步变短(扫描/过滤)时遗留旧下标越界,指向不存在的 option id,SR 失去当前项语义。→ 条件改为 `selectedIndex >= 0 && selectedIndex < filtered.length`。+TDD(两 modal:selectedIndex 越界→aria-activedescendant=null)。typecheck + 58 测试 + lint 绿。**沉淀:combobox 三属性下标安全收口 —— aria-activedescendant 必须双重校验(有结果 + 下标在界内),不能只判有无结果;「保留旧 results + 异步过滤变短」是越界的典型成因。combobox ARIA 完整契约:expanded 恒 true(A100)/controls 仅 listbox 存在(A101)/activedescendant 仅下标有效(A111),三者随结果数协同。**
- **A112 修(P2,A111 后续:虚拟列表 aria-activedescendant 指向未挂载 option)**:`CommandPalette.tsx:201` + `QuickOpenModal.tsx:196` aria-activedescendant 只校验下标在逻辑界内,未校验该 option 是否在 `rowVirtualizer.getVirtualItems()` 渲染窗口内 → 结果多时选中项逻辑有效但 DOM 未挂载(scrollToIndex 滚入前瞬态),引用悬空。→ 加 `activeOptionRendered = getVirtualItems().some(v => v.index === selectedIndex)`,aria-activedescendant 再加该校验。+TDD(新 spec 用窄渲染窗口 mock[0..2]:selectedIndex=30 有效但窗口外→null;=1 窗口内→指向真实挂载 option)。typecheck + 2 测试 + lint + bdd:index 重建(170 主题)绿。**沉淀:虚拟化列表 + aria-activedescendant 必须校验「option 真在 DOM」——逻辑下标有效不等于元素已渲染(虚拟窗口 + 异步 scroll);测试虚拟窗口边界须用窄渲染 mock(只渲染 0..2)而非默认全渲染 mock。combobox ARIA 契约最终层:activedescendant 三重门(有结果 A101 / 下标在界 A111 / option 已挂载 A112)。**
- **A113 修(P2,ARIA menu 子结构不完整)**:`ExplorerHeader.tsx:205` role="menu" 内混入普通 div 分隔线 + 「最近打开」分组标题,未标 separator/group → ARIA menu 子结构不完整,AT 可能把装饰分隔线/分组标题当异常菜单内容。→ 分隔线 div 标 `role="separator"`(合法 menu 子结构);最近项用 `role="group"` + `aria-label`(组名)包裹,视觉标题 `aria-hidden`(纯视觉,组名经 aria-label 提供避免重复朗读)。+TDD(menu 内有 role=separator + 命名 role=group 包住 menuitem)。useMenuKeyboard 的 querySelectorAll('[role=menuitem]') 递归,嵌套 group 不影响键盘漫游。typecheck + 19 测试 + lint 绿。**沉淀:role=menu 容器的子结构须完整 —— 分隔线 role=separator、逻辑分组 role=group(命名)、装饰标题 aria-hidden;裸 div 混入 composite 容器(menu/tablist/listbox)会破坏 AT 对结构的理解。结构分组族延伸到 menu 内分组(A97-99 group / A107-108 tablist / A113 menu separator+group)。**
- **A114 修(P2,aria-label 挂无 role div 反模式:ToastViewport)**:`ToastViewport.tsx:18` toast 容器 aria-label 挂在无语义普通 div 上 → label 不形成可导航区域,SR 只能靠单条 toast live 播报,无法按区域回看通知栈。→ 容器加 `role="region"`(保留本地化 aria-label),成为命名「通知」region。+TDD(viewport role=region + 非空 aria-label)。typecheck + 26 测试 + lint 绿。**沉淀:「aria-label 挂无 role 元素」反模式第四例(A40/A74/A91/A103 是状态/名称版,A114 是区域版)—— aria-label 在无 role 的 div 上不形成可访问名/区域;容器要被 AT 当区域回看须 role=region(或其它 landmark)+ 名称。凡 `<div aria-label=...>` 无 role 都查:要么补 role,要么删无效 label。**
- **A115 修(P2,A50 同族:MCP 复制 IPC reject 静默)**:`StatusBar.tsx:56` handleCopyMcpConfig 只处理 `!r.ok` 和剪贴板写入失败,但 `coApi.mcp.getStdioConfig()` promise reject(IPC 异常)未捕获 → onCopyMcp() reject,点击处 `void onCopyMcp()` 丢弃异常 → 无失败文本/无 role=status(A51)播报,用户以为无响应。→ 整体 try/catch,任何 reject 归 'fail'(复用既有失败按钮文案 + A51 live region)。+TDD(getStdioConfig reject → 「复制失败」)。typecheck + 23 测试 + lint 绿。**沉淀:void promise 静默失败族(A50/A58/A115)—— async 操作的「数据获取 reject」与「!ok 业务失败」是两条独立失败路径,只处理后者会漏前者;整体 try/catch 比逐句处理更稳。凡 `void someAsync()` 调用点,被调函数内部必须捕获所有 reject 路径(IPC/网络/数据获取),否则异常经 void 蒸发。**
- **A116 修(P2,A50/A115 同族:createUserTerminal IPC reject 静默)**:`create-user-terminal.ts:41` coApi.terminal.create 只处理 `{ok:false}` 业务失败,IPC promise reject 直接抛;Dock 入口 HeaderActions `onClick={() => addPanel(...)}` 不 await/catch → 用户点「新建终端」若 IPC reject,菜单不关、无 toast/live 反馈。→ 整体 try/catch,reject 走 `notify.error(create_failed)` + 返 null(与 !ok 路径统一)。+TDD(create reject → notify.error 一次 + 返 null + 不 setPendingFocus)。typecheck + 7 测试 + lint 绿。**沉淀:void promise 静默失败族第三处收口(A50 popout/A58 agent respond/A115 MCP copy/A116 新建终端)—— 共用 helper 即便已处理业务失败(!ok)仍可能漏 IPC reject;返 nullable 的 async helper 须整体 try/catch 把「reject」也转成「带反馈的 null」,因为调用点常 void 丢弃。这是 a11y 审计在「静默失败」方向的长尾,与早期数据安全审计(topic-54)的 promise reject 族同根但聚焦"用户可感反馈"。**
- **A117 修(P2,A50/A116 同族:Markdown 外链 openExternal void 丢弃)**:`EditorPanel.tsx:91` Markdown 外链点击 `void coApi.shell.openExternal(url)` 丢弃 {ok:false} 和 reject → URL 被白名单拒绝/系统打开失败时无 toast/live 反馈,用户感知链接「没反应」。→ `.then` 检查 IpcResult.ok + `.catch` reject,失败 `notify.error(localizeErrorByCode(code,message))`。+TDD(新 spec 用 hoisted mock 捕获 MilkdownEditor.onLinkClick:ok:false→notify.error / reject→notify.error / ok:true→不报错)。typecheck + 3 测试 + lint + bdd:index(171 主题)绿。**沉淀:void promise 静默失败族第五处(A50/A58/A115/A116/A117);openExternal 这类「点击即触发的外部副作用」有 {ok:false}(白名单拒绝)+ reject(IPC)两条失败路径,都要 notify。测试内部回调(经子组件 prop 传入的 onLinkClick)用 vi.hoisted + mock 子组件捕获 prop 调用 —— vi.mock 工厂提升,引用变量必经 vi.hoisted。**
- **A118 修(P2,A117 兄弟入口:终端 WebLinksAddon openExternal void)**:`useTerminal.ts:256` 终端 WebLinksAddon 外链点击 `void coApi.shell.openExternal(url)` 丢弃 {ok:false}/reject → URL 被拒绝/系统打开失败无反馈(与 A117 EditorPanel 同模式的兄弟入口)。→ 同步修法:`.then` 检查 ok + `.catch` reject,失败 `notify.error(localizeErrorByCode)`(导入 notify+localizeErrorByCode)。+TDD(既有 terminal-url-click spec 加 ok:false→notify.error / reject→notify.error)。typecheck + 3 测试 + lint 绿。**沉淀:外链 openExternal 静默失败两入口收口(A117 Markdown 编辑器 / A118 终端 WebLinks)——同一外部副作用 API 在多处调用,修一处必 grep 所有 `openExternal` 调用点;codex 明示「与 EditorPanel 同步」即兄弟入口提示。void promise 静默失败族至此六处(A50/A58/A115/A116/A117/A118)。**
- **A119 修(P1/P2,键盘可达性:Dock tab 不可键盘操作)**:`SharedTab.tsx:129` Dock tab 根是 div + cursor-pointer + pointer handlers,无 role/tabIndex/键盘激活 → 键盘用户无法聚焦切换 Dock tab,SR 拿不到 tab/selected 语义。→ 根加 `role="tab"` + `aria-selected={active}` + roving `tabIndex={active?0:-1}` + onKeyDown(Enter/Space→`api.setActive()`)。+TDD(active tab role=tab/aria-selected=true/tabindex=0;inactive aria-selected=false/tabindex=-1;Enter/Space→setActive)。makeApi mock 补 setActive。typecheck + 11 测试 + lint 绿。**沉淀:第三方组件(dockview)渲染的自定义 tab 也须补键盘可达性 —— pointer-only 交互(div+onPointerDown)对键盘/SR 完全不可达,须 role=tab+aria-selected+roving tabIndex+Enter/Space 激活(调库 api.setActive)。这是 a11y 审计「键盘可达性」方向(A62/A63 捕获 modal 后)的又一类:第三方容器内的自写交互元素。**
- **A120 修(P2,A119 后续:Dock tablist 方向键模型不完整)**:`SharedTab.tsx:129` A119 给 tab 加了 role=tab + roving tabIndex,但 handleKeyDown 只处理 Enter/Space,缺 ArrowLeft/Right/Home/End → 键盘 Tab 只落到 active tab,无法在 tab strip 间切换,WAI-ARIA tablist 键盘模型不完整。→ 补完整方向键:ArrowRight/Left 经 `api.group.panels`(同组有序 panel)循环找相邻 panel 并 `setActive()`,Home/End 到首尾,焦点经 `data-panel-id` rAF 跟随到新 active tab。单 tab(panels≤1)不响应。+TDD(ArrowRight→next/ArrowLeft→prev/Home→首/End→尾 各 setActive;单 tab 不激活)。typecheck + 13 测试 + lint 绿。**沉淀:WAI-ARIA tablist 键盘模型完整性 = roving tabIndex(A119)+ Enter/Space 激活(A119)+ 方向键跨 tab 移动并激活 + Home/End + 焦点跟随(A120);跨独立渲染的 tab 组件做方向导航靠库的有序集合(dockview group.panels)+ data-id 定位焦点。复用 roving-tablist.ts 不适用(那是同父 DOM 查询;dockview tab 是分离组件,须经库 API)。**
- **A121 修(P2,A119/A120 后续:role=tab 孤立无父 tablist)**:`SharedTab.tsx:181` SharedTab 子项已 role="tab"(A119),但 dockview 生成的父 tab-strip 容器(`.dv-tabs-container`)无 role="tablist"/名称 → role=tab 孤立(非法 ARIA composite),AT 不知这些 tab 同属一组。→ SharedTab 挂载 effect 中 `rootRef.closest('.dv-tabs-container')` 给容器补 `role="tablist"` + 本地化 `aria-label`(新增 shell.tab.dock_tablist 三语言;幂等,同容器多 tab 重复设无副作用)。+TDD(渲染进 .dv-tabs-container → 挂载后该容器 role=tablist + 非空 aria-label)。typecheck + 14 测试 + lint 绿。**沉淀:第三方库(dockview)生成的 DOM 缺 ARIA 父容器语义时,在自控子组件挂载 effect 里 closest() 向上补 role/aria(setAttribute)是务实手段;role=tab/option/menuitem 等 composite 子角色必须有对应父容器(tablist/listbox/menu),否则是非法 ARIA。Dock tab 键盘可达性三连(A119 tab 语义+激活 / A120 方向键模型 / A121 父 tablist 容器)完整收口。**
- **A122 修(P2,A50 同族:语言设置 setStoreLocale void 丢弃)**:`LanguageFromSettings.tsx:37` 语言变更 `void setStoreLocale(value)`,setLocale(Promise<void>)在 coApi.i18n.setLocale {ok:false}/reject 时 throw → 被 void 丢弃。用户改语言后界面仍旧语言但控件显示新值,无 toast/live 反馈、状态不一致。→ `.catch` → `notify.error(t('settings.general.language.save_failed'))`(新增三语言)+ 回滚控件值到真实 storeLocale(value===storeLocale 后 effect 守卫不再重触发,无循环)。+TDD(setLocale reject → notify.error + values 回滚到 zh;既有 setLocale mock 改返 Promise.resolve 配合 .catch)。typecheck + 3 测试 + lint 绿。**沉淀:void promise 静默失败族第七处(A50/A58/A115/A116/A117/A118/A122);设置类异步持久化失败须 notify + 回滚 UI 控件态(否则控件显示新值但实际未改=状态撒谎);改 void→.catch 后,既有测试里返 undefined 的 mock 必须改返 Promise(否则 .catch 崩),与改组件 promise 处理同步修 mock(同 A50)。**
- **A123 修(P2,A29 同族:Dock tab 内按钮破坏 roving)**:`SharedTab.tsx:230/247` tab 根已 roving role=tab(A119),但 tab 内的关闭/退出最大化 IconButton 仍默认可 Tab 聚焦 → 键盘 Tab 进入每个(含 inactive)tab 内部按钮,破坏「一次 Tab 进 tablist、方向键移动」复合控件模型。→ 两 IconButton `tabIndex={-1}`,tab 根 handleKeyDown 补 Delete/Backspace → `api.close()`(同 TabNav A29)。+TDD(关闭按钮 tabindex=-1 + Delete/Backspace→api.close)。typecheck + 21 测试 + lint 绿。**沉淀:WAI-ARIA composite(tablist/menu/tree)内的次要按钮(关闭/操作)必须移出 Tab 顺序(tabIndex=-1)+ 提供等价键盘入口(Delete/Backspace 等),否则每个项的内部按钮都成 tab stop 破坏「一次进出 + 方向键移动」模型。Dock tab 键盘可达性四连(A119 tab 语义/A120 方向键/A121 父 tablist/A123 内部按钮 roving)完整;与 TabNav(A23/A28/A29)是孪生 composite。**
- **A124 修(P2,A123 后续:退出最大化按钮无键盘等价入口)**:`SharedTab.tsx:232` A123 把退出最大化按钮 tabIndex=-1 移出 Tab 顺序,但只给关闭按钮补了 Delete/Backspace,退出最大化无键盘入口 → 最大化态键盘用户无法退出缩放(现有 terminal.zoom.toggle 命令只 guard terminal active panel,不覆盖所有显示该 indicator 的 Dock group)。→ tab 根 handleKeyDown 最大化态 Escape → `containerApi.exitMaximizedGroup()`(try/catch)。+TDD(maximized 态 Escape→exitMaximizedGroup;非 maximized 态 Escape 不调)。typecheck + 17 测试 + lint 绿。**沉淀:把次要按钮移出 Tab 顺序(tabIndex=-1,A123)后,每个被移出的可点击操作都须有键盘等价入口(close→Delete/Backspace,exit-maximize→Escape)——移出焦点 ≠ 移除功能,否则键盘用户彻底失去该操作。A123 移出按钮 + A124 补等价键是配对动作。Dock tab 键盘可达性五连(A119-A124)。**
- **A125 修(P2,A121 后续:dock tablist aria-label 不随 locale 更新)**:`SharedTab.tsx:86` A121 用静态 `t` + 「仅缺失时设」守卫给 dockview 容器补 aria-label → ① 静态 t 在 locale 切换时组件不重渲;② 守卫导致即便重渲也不更新 → SR 读旧语言 Dock 组名。→ 组件改用 `useT()`(订阅 locale,使关闭/退出缩放/tablist 所有可访问名随语言实时更新)+ effect 去掉 aria-label 守卫(每次重写)。+TDD(en→'Panel tabs',setLocale('zh')+notifyLocaleChange→'面板标签')。typecheck + 18 测试 + lint 绿。**沉淀:本地化可访问名须用 useT(订阅)而非静态 t,否则 locale 切换后停留旧语言;给第三方 DOM setAttribute 补 aria-label 时不能用「仅缺失时设」守卫(挡住 locale 更新),应每次(随 useT 重渲)重写。i18n×a11y:可访问名的本地化既要正确翻译(A105/A106)也要随 locale 实时更新(A125)。测试 locale 实时切换须 setLocale + notifyLocaleChange(模块 setLocale 不自动 notify,生产由 settings.store 配对调)。**
- **A126 修(P2,A58 同族:fs-scope 决定回传无 catch)**:`usePluginFsScopeRequests.ts:39` fs-scope 授权决定回传链 `.then(() => _scopeDecision(...))` 无 .catch → requestFsScope 或 _scopeDecision reject(IPC 异常)时,用户已点授权/拒绝、弹窗已关,失败被静默丢弃,main 侧 scope 请求一直 pending/超时,用户和 SR 都不知决定未生效。→ 加 `.catch` → `notify.error(translate('permissions.fs_scope.respond_failed'))`(新增三语言)。+TDD(新 spec hoisted mock:_scopeDecision reject/requestFsScope reject → notify.error;正常决定不报错)。typecheck + 3 测试 + lint + bdd:index(172 主题)绿。**沉淀:void promise 静默失败族第八处(A50/A58/A115/A116/A117/A118/A122/A126);授权/决定回传链路(agent A58 / fs-scope A126)是安全敏感的静默失败 —— 决定丢失但 UI 假装已处理,主进程干等超时;凡 `void store.method().then(ipc回传)` 的用户决定应答都须 .catch + notify。**
- **A127 修(P2,A117 同分支兄弟:Markdown 本地文件链接 openFileByPath void)**:`EditorPanel.tsx:89` handleLinkClick 的 file 分支 `void openFileByPath(target.absPath)` 丢弃 FileOpResult 和 reject(A117 修了 else 外链分支,这是 if 文件分支)→ 目标文件不存在/读取失败时无反馈。→ `.then` 检查 r.ok + `.catch` reject → `notify.error(localizeErrorByCode)`(镜像外链分支 + QuickOpen openFile 处理)。+TDD(file 分支 {ok:false}/reject → notify.error;A117 spec 加 useEditorFile mock)。typecheck + 5 测试 + lint 绿。**沉淀:同一 handler 的两条分支(A117 外链 else / A127 文件 if)都可能 void 丢弃失败 —— 修一个分支必查同 handler 的所有分支;openFileByPath 是已有 FileOpResult 契约的 helper(QuickOpen 早已正确处理 ok/reject),编辑器链接点击复用却漏了。void promise 静默失败族九处(A50/A58/A115-A118/A122/A126/A127)。**
- **A128 修(P2,静默失败:关闭终端 panel remove 失败只 console.warn)**:`wrap-panel-close.ts:80` 用户关闭 terminal panel 时 `coApi.terminal.remove(sessionId)` 的 ok:false/reject 只 console.warn → panel 已从界面消失但 PTY/session 可能未关,用户/SR 无失败反馈。→ ok:false 与 reject 都补 `notify.error(translate('errors.terminal.remove_failed',{code}))`(新增三语言,保留 console.warn 作日志)。+TDD(新 spec hoisted mock:remove {ok:false}/reject→notify.error;ok:true 不报错)。typecheck + 8 测试 + lint + bdd:index 绿。**沉淀:静默失败族「只 console.warn 无用户反馈」(A46-A50 早期 + A128)—— 用户触发的破坏性/状态变更操作(关闭终端=终止 PTY)失败必须 notify;console.warn 是开发者日志,不是用户反馈。a11y「静默失败」方向至此覆盖 catch-only-warn / void promise / IPC reject 三类共约 20 处。**
- **A129 修(P2,A116/A128 同族:spawnLeaf create reject 致终端永久 loading)**:`spawnLeaf.ts:132` run() 内 `await coApi.terminal.create` 无 try/catch + 外层 `void run()` → IPC reject 时 modulePending 不清、不派 SET_PTY_FAIL,终端 leaf 永久停在 loading,用户/SR 无反馈。(`!result.ok` 路径已派 fail,但 reject 路径绕过它。)→ try/catch 包 create:catch 中 modulePending.delete(清账)+ 非 cancelled 时派 SET_PTY_FAIL(若有 live dispatch)+ notify.error(create_failed)。+TDD(新 spec:create reject→SET_PTY_FAIL+notify+清 pending;cancelled 态 reject→静默清账不派 fail)。typecheck + 2 测试 + lint + bdd:index(174 主题)绿。**沉淀:状态机式 async(loading→success/fail)的 reject 路径必须与 !ok 失败路径走同样的「退出 loading + 派 fail + 反馈」—— 只处理 !result.ok 会漏 throw 路径,导致「卡在 loading 永不退出」(比无反馈更糟,UI 假装还在进行)。void promise 静默失败族第十处(A50/A58/A115-A118/A122/A126/A127/A129);状态机 loading 态尤须 finally/catch 保证必退出。**
- **A130 修(P2,A129 同族:初始终端会话恢复静默失败)**:`TerminalSessionsSync.tsx:62` 初始 `coApi.terminal.listSessions().then(...)` 无 .catch 且 !r.ok 静默早返 → IPC reject 产生未处理 rejection + 会话恢复失败时列表缺失无反馈。→ 拆分:reject(.catch)与 !r.ok 都 `notify.error(sessions_restore_failed)`(新增三语言);被实时推送取代(sawPush)或 cancelled 则跳过(非失败)。+TDD(reject/!ok→notify.error;late-list-no-clobber 无回归)。typecheck + 8 测试 + lint 绿。**沉淀:void promise 静默失败族第十一处(A50/A58/A115-A118/A122/A126/A127/A129/A130);初始 hydration/恢复链路失败=「界面假就绪」(列表空但无错误),须 notify;但要区分「真失败」与「被更权威的实时推送取代」(sawPush 守卫)——后者不是失败不应 notify。**
- **A131 修(P2,A130 同族:Dock 布局恢复静默失败 + reject 中断 onReady)**:`DockShell.tsx:124` `await coApi.layout.read()` 无 try/catch → reject 会中断整个 onReady(连 applyDefaultLayout 都跳过,布局彻底空);!ok / fromJSON 失败只 console.warn 静默落回默认。→ try/catch 兜底 read(reject 不再中断),read 失败(reject/!ok)或 fromJSON throw → `notify.error(layout_restore_failed)`(新增三语言);首次无持久布局(ok+data=null)不算失败,默认布局静默应用。+TDD(read reject/!ok→notify.error;首次 null→不报错;复用 DockShell+DockviewReact mock,addPanel 须返带 api.setActive 的对象否则 applyDefaultLayout 抛)。typecheck + 5 测试 + lint + bdd:index 绿。**沉淀:async 初始化回调(onReady)里裸 await 失败会中断整个回调后续(比静默更糟,连兜底都不跑)——初始化链路的每个 await 都要 try/catch 保证「失败也能完成初始化(应用默认 + 反馈)」;区分真失败 vs 首次空数据(null≠错误)。a11y 静默失败方向覆盖 init/restore 三处(A130 终端会话/A131 dock 布局,均「假就绪/被重置」无反馈)。**
- **A132 修(P2,A131 同族:Dock 布局保存失败静默)**:`DockShell.tsx:107` `writeDockLayoutSnapshot` 写入失败只 console.warn,且 debounce 自动保存路径不处理 reject → 用户移动/关闭 panel 后布局未持久化,界面当下看似成功,重启才发现布局丢失。→ 加 `notifyOnFail` 参数:自动保存(debounce,line 178)传 true → !ok/reject **限流** notify(5s 内同类只播报一次,避免 debounce 连续失败 toast 轰炸);关窗 flush(line 234)传 false → 保留「日志 + 抛出由调用方 try/catch」语义。+TDD(自动保存 write {ok:false} → 经 onDidLayoutChange→debounce→notify.error;复用 A131 harness 捕获 onDidLayoutChange callback)。typecheck + 4 测试 + lint 绿。**沉淀:高频自动保存(debounce)的失败反馈须限流(否则每次 debounce 失败一个 toast),关窗一次性 flush 可不限流/可只日志;失败反馈也要考虑频率(限流是反馈完整性与噪声的平衡,同 A93 loading 不持续噪声)。dock 布局 read(A131)+write(A132)双向反馈收口。**
- **A133 修(P2,A50 同族:Explorer inline rename reject 静默)**:`FolderTree.tsx:146` inline rename 的 fire-and-forget `void (async () => {...})()` 只处理 renameItem 返回的 ok:false,未 catch fs.rename IPC reject → 键盘用户重命名异常时静默失败无反馈。→ try/catch 包住 async body,reject → `notify.error(localizeErrorByCode(code,message))`。+TDD(新 spec 经 useTree mock 捕获 config.onRename:renameItem reject/ok:false 都 notify.error;mock mutate-actions 的 renameItem)。typecheck + 2 测试 + lint + bdd:index(176 主题)绿。**沉淀:void promise 静默失败族第十二处(A50/A58/A115-A118/A122/A126-A130/A133);headless-tree 这类「sync 签名 callback 内 fire-and-forget async」尤其易漏 reject(只处理业务 ok:false,忘了 IPC throw)——凡 `void (async()=>{...})()` 闭包须整体 try/catch。测试 FolderTree 内联 callback 经 useTree mock 捕获 config(createTreeConfig 透传 onRename),再直接调用。**

## A134 — FolderTree onDropItems 拖放移动 fire-and-forget reject 无反馈 (P2)

- **文件**: `src/panels/Explorer/FolderTree.tsx:172` 内部多选拖动 `onDropItems`
- **问题**: A133(inline rename)同族。`makeUniqueDestPicker(destDir)` 在 try 外、move 循环只处理 `!r.ok`,`coApi.fs.move` / `coApi.fs.listDir`(picker 目标名探测)的 IPC reject 直接丢到 unhandled promise(`void (async()=>…)()`)。拖放移动失败时无 toast / live region 反馈,用户只见文件没动或部分移动后状态不明。
- **修复**: 把 `makeUniqueDestPicker` 与 move 循环都包进同一 try;新增 `catch`→`notify.error(localizeErrorByCode(code, msg), { code })`;保留 `finally` 部分成功刷新已移动目录(movedAny 才刷,语义不变)。catch 取 `(err as {code?}).code ?? 'EXCEPTION'`。
- **测试**: `src/__tests__/folder-tree-rename-feedback/rename-reject-feedback.spec.tsx` 新增 `a11y(A134)` describe 两例 — `coApi.fs.move` reject、`makeUniqueDestPicker`(listDir)reject 均断言 `notify.error` 被调一次(经 config.onDrop + folderTarget 驱动)。
- **沉淀**: silent-failure 族跨入口必查所有平行 fire-and-forget 分支 —— inline rename(onRename)修了,同文件的拖放(onDropItems)是兄弟缺口;`await` 的对象不只 move,还有 try 外的目标名探测(picker),包 try 必把前置 await 一并纳入。

## A135 — FolderTree onPaste 剪贴板粘贴 fire-and-forget reject 无反馈 (P2)

- **文件**: `src/panels/Explorer/FolderTree.tsx:433` 剪贴板 `onPaste`(cut→move / copy)
- **问题**: A134(onDropItems)/A133(onRename)同族,第三个兄弟入口。`makeUniqueDestPicker(destDir)` 在 try 外、循环只处理 `!r.ok`,`coApi.fs.move`/`copy`/`listDir`(picker)的 IPC reject 直接丢到 unhandled promise(`void (async()=>…)()`)。粘贴失败无 toast/live region 反馈,且 cut 场景可能丢上下文(只贴一半状态)。
- **修复**: 把 `makeUniqueDestPicker` 与 move/copy 循环都包进同一 try;新增 `catch`→`notify.error(localizeErrorByCode(code, msg), { code })`;保留 `finally` 部分成功剪贴板 prune + 刷新(语义不变)。
- **测试**: 同 spec 新增 `a11y(A135)` describe 三例 — cut move reject、copy copy reject、makeUniqueDestPicker(listDir)reject 均断言 `notify.error` 调一次(经 ContextMenu mock 捕获 contextActions.onPaste + 真实 clipboard-store 驱动)。
- **沉淀**: silent-failure 拖放/粘贴族在 FolderTree 有三个平行 fire-and-forget 入口(onRename / onDropItems / onPaste),修一个必 grep 同文件所有 `void (async`;onPaste 不在 TreeConfig 而在 contextActions(经 ContextMenu),测试 seam 须 mock ContextMenu 捕 actions(区别于 onRename/onDropItems 经 useTree config)。

## A136 — FolderTree handleDrop root-drop 分支 reject 无反馈 (P2)

- **文件**: `src/panels/Explorer/FolderTree.tsx:557` `handleDrop` 内部拖到空白(root level)分支
- **问题**: A135(onPaste)/A134(onDropItems)/A133(onRename)同族,第四个平行入口。`handleDrop` 是绑在容器 div 的 `async` 事件处理器(React 不 await),`makeUniqueDestPicker(root)` 在 try 外、循环只处理 `!r.ok`,`coApi.fs.move`/`listDir` 的 IPC reject 直接成 unhandled rejection,拖到空白处批量移动失败无 toast/live region 反馈。
- **修复**: 把 `makeUniqueDestPicker(root)` 与 move 循环都包进同一 try;新增 `catch`→`notify.error(localizeErrorByCode(code, msg), { code })`;保留 `finally` 部分成功刷新 root/源父目录 + 剪贴板 prune(语义不变)。
- **测试**: 同 spec 新增 `a11y(A136)` describe 两例 — move reject、makeUniqueDestPicker(listDir)reject 均断言 `notify.error` 调一次(经 fireEvent.drop 容器 + fakeTree getState 注入 dnd.draggedItems 驱动 root-drop 分支)。同步加宽既有 `49-polish-bugfixes/root-drop-partial-refresh.spec.ts` 的源码切片窗口 2400→3200(catch 块插入使 finally 后移)。
- **沉淀**: FolderTree 文件移动 fire-and-forget 族至此四入口齐修(onRename / onDropItems / onPaste / root-drop handleDrop);`async` React 事件处理器与 `void (async)()` 同样会丢 reject(React 不 await onDrop)。源码 grep 型契约测试(indexOf marker + 定长 slice)对插入新代码脆弱:加 catch 必复核所有按字符窗口断言的测试,窗口要留余量。

## A137 — performDrop 外部 OS 文件 drop writeBinary reject 未捕获 (P2)

- **文件**: `src/panels/Explorer/drop-handlers.ts:102` `performDrop()`
- **问题**: A133-A136 同族(文件操作 fire-and-forget)的外部文件 drop 变体。`file.arrayBuffer()`(line 92)已有 try/catch 归类到 `failed`,但 `fs.writeBinary(targetPath, bytes)`(line 102)只处理 `r.ok`/`!r.ok`,IPC reject(抛错而非返回 {ok:false})未捕获 → performDrop 整体 reject;而调用点 `FolderTree.handleDrop` 外部文件分支(line 624 `await performDrop`)是 async 事件处理器不被 React await → unhandled rejection + 该文件不计入 `r.failed` → 批量错误 toast 漏报,用户不知哪些文件没写成功。
- **修复**: writeBinary 包 try/catch,reject → `failed.push({ name, code: 'WRITE_ERROR', message: errorMessage(err) })` + `continue`(镜像上方 arrayBuffer 的 READ_ERROR catch)。performDrop 自此恒返回 DropResult 不 reject,调用点已有的 `r.failed` 批量 toast 聚合(line 631-642)自动覆盖。
- **测试**: `src/__tests__/explorer-dropzone/drop-handlers.spec.ts` 新增「writeBinary reject → failed WRITE_ERROR(不 reject)」—— 三文件中第二个 writeBinary mockRejectedValueOnce,断言 `r.ok=false`、written=[a,c]、failed=[{name:'b',code:'WRITE_ERROR'}]。
- **沉淀**: 同一循环里的多个 await(arrayBuffer + writeBinary)易只给前者加 catch、漏后者 —— 批处理函数「部分失败归类」契约要求每个可 reject 的 await 都归类到 failed,否则一个 reject 让整批 reject + 调用点聚合失效。纯逻辑 helper(drop-handlers.ts)的 reject 归类比 UI 层 try/catch 更上游、更易测(无需渲染)。

## A138 — removeItems trash/remove reject 未捕获 (P2)

- **文件**: `src/panels/Explorer/mutate-actions.ts:69` `removeItems()`
- **问题**: A137 同族(批处理 helper 部分失败归类)。循环里 `deps.fs.trash(p)` / `deps.fs.remove(p)` 只处理 `!r.ok`,IPC reject(抛错而非返回 {ok:false})未捕获 → removeItems 整体 reject;而调用点 `FolderTree.onTrash`(line 392)是 `void (async()=>)()` fire-and-forget,reject 即成 unhandled rejection + 该项不计入 `result.failures` → 批量删除/移废纸篓错误的 toast 漏报(line 413-424 永不进入)。
- **修复**: trash/remove 调用包 try/catch,reject → `failures.push({ path, code: (err.code ?? 'EXCEPTION'), message })` + continue(镜像 !r.ok 分支)。removeItems 自此恒返回 RemoveResult 不 reject,onTrash 已有的 `!result.ok` 逐项 notify.error 自动覆盖。
- **测试**: `src/__tests__/explorer-mutate/mutate-actions.spec.ts` 新增两例 —— trash reject(带 code:'EIO')归类 failures + 成功项父仍刷;remove(硬删)reject 无 code → code='EXCEPTION'。均断言 removeItems 不 reject。
- **沉淀**: 「IPC 调用只处理 ok:false、漏 reject」是 silent-failure 族在批处理 helper 的统一形态(A137 performDrop / A138 removeItems);凡 `await fs.X()` 后只判 `r.ok` 的循环都须问「X reject 谁兜」。纯 helper 归类 reject 比 UI try/catch 更上游,且调用点已有的 failures 聚合逻辑零改动即生效。

## A139 — createNewFile / createNewDir createFile/createDir reject 未捕获 (P2)

- **文件**: `src/panels/Explorer/mutate-actions.ts:89/101` `createNewFile()` / `createNewDir()`
- **问题**: A138/A137 同族。两 helper 只处理 `deps.fs.createFile`/`createDir` 的 `!r.ok`,IPC reject(抛错而非返回 {ok:false})未捕获 → helper 整体 reject;而调用点 `FolderTree.submitCreate` 是 fire-and-forget async,reject 即 unhandled rejection + 不进 `!r.ok` 分支 → 新建文件/文件夹失败无 toast/live region 反馈。
- **修复**: 抽 `toActionFail(err)` 把 reject 归类成 `ActionFail`(code 取 `err.code ?? 'EXCEPTION'`,message 取 err.message);两 helper 的 createFile/createDir 调用包 try/catch → catch return toActionFail。helper 自此恒返回 ActionResult 不 reject,submitCreate 已有的 `!r.ok` notify.error 自动覆盖。
- **测试**: `src/__tests__/explorer-mutate/mutate-actions.spec.ts` 新增两例 —— createFile reject(带 code ENOSPC)→ ActionFail 不刷;createDir reject(无 code)→ code='EXCEPTION'。
- **沉淀**: silent-failure 族「IPC 调用只判 ok:false、漏 reject」在 mutate-actions 全四个动作齐修(rename 历史/remove A138/createFile+createDir A139);抽 `toActionFail` 复用避免每处手写 catch 漂移。同类批量收口:一个 helper 文件里所有 `await deps.fs.X()` 后只判 r.ok 的都是同款缺口,一轮 grep 全覆盖。

## A140 — onOpenInTerminal「在终端打开」terminal.create reject 无反馈 (P2)

- **文件**: `src/panels/Explorer/FolderTree.tsx:360` `onOpenInTerminal`
- **问题**: A139/A138/A137 同族。`void (async()=>)()` fire-and-forget 里 `coApi.terminal.create()` 只处理 `!r.ok`,IPC reject(抛错而非返回 {ok:false})与后续动态 `import('@/shell/dock/dock-api-ref')` 的 reject 均未捕获 → unhandled rejection,右键「在终端打开」失败时无 toast/live region 反馈。
- **修复**: 整个 async body 包 try/catch;catch → `notify.error(localizeErrorByCode(code, msg), { code })`,code 取 `err.code ?? 'EXCEPTION'`。覆盖 create reject 与 import reject 两条路径。
- **测试**: 同 spec 新增 `a11y(A140)` —— `coApi.terminal.create` mockRejectedValue,经 ContextMenu mock 捕 `contextActions.onOpenInTerminal('/repo/sub')`,断言 notify.error 调一次。
- **沉淀**: fire-and-forget async 里不止首个 await(terminal.create)会 reject,后续 `await import(...)` 动态加载也会 —— 包 try/catch 须裹整个 body 而非只裹第一个 await。Explorer contextActions 系列(onTrash/onPaste/onRename/onOpenInTerminal)的 fire-and-forget 缺口至此基本扫齐。

## A141 — useTerminalDragDrop 终端拖放 getPathForFile/terminal.write reject 无反馈 (P2)

- **文件**: `src/panels/Terminal/useTerminalDragDrop.ts:88` `onDrop` 内 `void (async()=>)()`
- **问题**: A140 同族,扩到 Terminal。fire-and-forget async 里 `coApi.window.getPathForFile(file)`(line 93,循环内)与 `coApi.terminal.write(...)`(line 109)的 IPC reject 此前未捕获 → unhandled rejection,终端拖放文件失败时无 toast/live region 反馈(write 只处理 !r.ok,getPathForFile 全无兜底)。
- **修复**: 整个 async body 包 try/catch;catch → `notify.warn(t('panels.terminal.drag_drop.write_failed'))`(复用既有 key,无需新增 i18n)。覆盖 getPathForFile reject 与 write reject 两路径。
- **测试**: `src/__tests__/terminal-drag-drop/drag-drop.spec.tsx` 新增 S17(write mockRejectedValue → 提示 write_failed)、S18(getPathForFile 抛错 → write_failed 且 write 未被调)。
- **沉淀**: silent-failure fire-and-forget 族跨模块(Explorer + Terminal)统一形态;循环内的 await(getPathForFile)与循环外的 await(write)都要在同一 try 内。复用既有失败 key 而非新造,避免 catalog 膨胀。A133-A141 共 9 处,横跨 Explorer contextActions / 纯逻辑 helper / Terminal 拖放。

## A142 — handleFileOpen 点击文件 openFileByPath reject 无反馈 (P2)

- **文件**: `src/panels/Explorer/FolderTree.tsx:313` `handleFileOpen`
- **问题**: A141 同族。点击文件是 Explorer 主操作。`await openFileByPath(path)` 只处理 `!r.ok`(已有 open_failed notify),但 reject(抛错而非返回 {ok:false})未捕获 → FileRow onClick 的 fire-and-forget 调用变 unhandled rejection,打开异常时「点了没反应」无 toast/live region 反馈。
- **修复**: handleFileOpen body 包 try/catch;catch 复用 `!r.ok` 同款 `quick_open.open_failed` 提示(code 取 `err.code ?? 'EXCEPTION'`,message 取 err.message)。deps 保持 `[openFileByPath]`(`t` 是静态导入,stable)。
- **测试**: 同 spec 新增 `a11y(A142)` 两例 —— openFileByPath reject、{ok:false}(既有行为保持)均断言 notify.error 调一次。为驱动真实 handleFileOpen,把 spec 的 `useEditorFile.openFileByPath` / virtualizer / `tree.getItems` 改为 hoisted 可注入,渲染单行文件 + fireEvent.click(getByText) 触发 FileRow onClick → handleFileOpen。
- **沉淀**: 「只判 r.ok、漏 reject」族至此扫到 Explorer 主交互(单击打开);UI 事件处理器(onClick/onDrop)调用的 async 即便不是 `void (async)()` 显式包裹,经事件回调 fire-and-forget 也会丢 reject。测 row 级 onClick 须把 tree.getItems + virtualizer + openFileByPath 三处 mock 注入一行才能驱动真实回调。A133-A142 共 10 处。

## A143 — useTerminal readHistory 失败不清 loading + 无 .catch (P2)

- **文件**: `src/panels/Terminal/useTerminal.ts:374` `readHistory().then`
- **问题**: 双族交汇 ——「只判 r.ok 漏 reject」(A141 族)+「isReady 终态未覆盖所有出口」(第八 session exit-clears 族)。lazy-mount 终端的初始输出(prompt)在 PTY spawn 后、panel mount 前产出,只存在于 history;`readHistory().then` 在 `!r.ok` 时只 `return` 不清 loading,且全无 `.catch`。history 读失败(reject 或 !ok)→ onData 不会补发那段初始输出 → firstData 恒真 →「Starting shell…」spinner 永久悬浮在可用终端上 + 无任何反馈;reject 还是 unhandled rejection。
- **修复**: 抽 `clearLoadingOverlay()`;`.then` 的 `!r.ok` 分支 → clearLoadingOverlay + `notify.error(localizeErrorByCode(r.code, r.message))`;新增 `.catch` → clearLoadingOverlay + notify.error(code 取 err.code ?? 'EXCEPTION')。空历史(ok + 无 data)仍 `return` 留 loading 给 onData(prompt 尚未产出的正常态,不误清不误报)。
- **测试**: 新建 `src/__tests__/terminal-readhistory-failure-feedback/readhistory-failure-clears-loading.spec.ts`(镜像 exit-clears harness + mock notify)三例 —— {ok:false}/reject 均清 isReady + notify.error 一次;空数据留 loading 不报。
- **沉淀**: 状态机的 set-true 出口必须覆盖所有终态(onData/onExit/history-success/**history-failure**);失败路径不仅要 notify,还要负责退出 loading,否则「成功才退 loading」会把失败永久卡在 loading。新增测试目录须 `pnpm bdd:index`;复用既有 useTerminal mock harness 时 SearchAddon mock 必带 onDidChangeResults(否则 doInitXterm 抛)。A133-A143 共 11 处。

## A144 — useTerminal 用户输入 terminal.write 失败无反馈 (P2,限流修)

- **文件**: `src/panels/Terminal/useTerminal.ts:409` `term.onData` → `void coApi.terminal.write`
- **问题**: A141 同族。用户每次按键 `term.onData` → `coApi.terminal.write(termId, outgoing)` 此前 fire-and-forget,既不判 `ok:false` 也无 catch。写入失败(PTY 已死 / IPC 异常)时用户键入「没反应」却无任何 toast/live region 反馈。
- **修复**(避免过度设计): 每个按键都 write,无差别弹 toast 会刷屏 —— 抽 `notifyWriteFail(code, message?)` 带 **3s 限流**(`lastWriteFailAt`,连续失败只提示一次,对齐 A132 dock autosave 限流思路);write 加 `.then(!ok→notifyWriteFail)` + `.catch(notifyWriteFail)`。`localizeErrorByCode` 第二参必填 → `message ?? code` 兜底。
- **测试**: 新建 `src/__tests__/terminal-input-write-feedback/input-write-reject.spec.ts` 四例 —— {ok:false}/reject 各 notify 一次、ok 不弹、连续三次失败按键 3s 内只提示一次(限流)。捕获 term.onData 回调模拟键入。
- **沉淀**: 高频事件(每按键)的失败反馈必须限流,否则「修 silent failure」会变「toast 刷屏」(极简原则:反馈要有但不刷屏)。这区别于低频操作(open/trash/paste)的无条件 notify。A133-A144 共 12 处。

## A145 — revealPathOrNotify reveal reject 未捕获 (P2)

- **文件**: `src/panels/Explorer/reveal-or-notify.ts:11` `revealPathOrNotify()`
- **问题**: A141 同族。`await coApi.fs.reveal(path)` 只处理 `!r.ok`(已有 notify),但 reveal reject(抛错而非返回 {ok:false})未捕获 → 菜单「在 Finder/资源管理器中显示」onClick 的 fire-and-forget 调用变 unhandled rejection,reveal 异常时菜单关闭却无 toast/live region 反馈(讽刺:该函数本就是 A49 为「不静默」而建)。
- **修复**: body 包 try/catch;catch → `notify.error(localizeErrorByCode(code, msg ?? code), { code })`,code 取 `err.code ?? 'EXCEPTION'`。
- **测试**: `src/__tests__/explorer-mutate/reveal-or-notify.spec.ts` 新增「reject → notify.error(不抛出)」—— reveal mockRejectedValue(带 code EIO),断言 resolves + notify 一次 + 含 message。
- **沉淀**: 连「为不静默而生」的 helper(A49 revealPathOrNotify)自己也漏了 reject 兜底 —— `await fs.X()` 只判 r.ok 的形态在全仓任何位置都可能复现,与函数语义无关。A133-A145 共 13 处。

## A146 — selectDirectory 切换/打开文件夹 !r.ok / reject 无反馈 (P2,两兄弟入口)

- **文件**: `src/panels/Explorer/ExplorerHeader.tsx:96` `switchFolder` + `src/panels/Explorer/EmptyWorkspace.tsx:20` `open`(codex 建议一并查,确为同款)
- **问题**: A141 同族。两处 `await coApi.fs.selectDirectory()` 都只 `if (r.ok && r.data) setRoot(...)`,`!r.ok` 静默、reject 无 catch(仅 try/finally 清 busy)→ 选目录失败时菜单关闭/按钮退 busy 却无 toast/live region 反馈,reject 还是 unhandled rejection。
- **契约确认**: `electron/main/ipc/fs.ipc.ts:145` 取消返回 `null` → IpcResult `{ok:true, data:null}`。故**取消 = ok+无 data = 正常 no-op 不报**;`!r.ok` = 真错误才 notify(避免每次取消都弹 toast)。
- **修复**: 两处 try/finally → try/catch/finally;`else if (!r.ok) notify.error(localizeErrorByCode(r.code, r.message))`;catch → notify.error(code 取 err.code ?? 'EXCEPTION')。两文件各补 notify + localizeErrorByCode import。
- **测试**: explorer-header spec +3(ok:false notify / reject notify / 取消不报);empty-workspace spec +3(同三态)。均 spy `notify.error`。
- **沉淀**: 「取消」与「失败」必须区分 —— 原生对话框取消是 ok+null 正常流,误当失败 notify 会每次取消刷 toast(同 A144 防刷屏精神)。修一处 selectDirectory 必 grep 所有 selectDirectory 调用点(ExplorerHeader + EmptyWorkspace 两兄弟,codex 主报一处但同族一起修)。A133-A146 共 15 处(含本轮两入口)。

## A147 — 点击「最近打开」目录无失效校验/反馈 (P2,两兄弟入口 + 共享 helper)

- **文件**: `src/panels/Explorer/EmptyWorkspace.tsx:78` + `src/panels/Explorer/ExplorerHeader.tsx:239`(recent root 列表项)
- **问题**: A146/A141 同族延伸。两处 recent root 列表项 onClick 直接 `setRoot(p)` 无校验。最近目录可能已删/移动/失权;切进后 FolderTree 的 dataLoader listDir 失败默认只 `onIpcWarn`→console.warn(FolderTree **未传** onIpcWarn notify),用户看到空白 Explorer 却无解释,「切了没反应」。
- **修复**: 新建共享 helper `src/panels/Explorer/open-recent-root.ts` `openRecentRootOrNotify(path, setRoot)` —— 先 `coApi.fs.listDir(path)` 探测,`!r.ok`/reject → `notify.error(localizeErrorByCode(...))` 且**不切**;ok 才 setRoot。两入口 onClick 改 `void openRecentRootOrNotify(p, setRoot)`。
- **测试**: 新建 `open-recent-root.spec.ts`(ok→setRoot / !ok→notify不切 / reject→notify不切);empty-workspace +1、explorer-header +1(点失效 recent → notify 不切);既有两 recent-click 测试改 async + installFs 加默认 listDir ok 桩。
- **沉淀**: 「设状态」与「状态可用」是两件事 —— setRoot 只改 store,目标可用性要么前置校验(本修),要么落地处 notify。前置校验避免切进半坏状态(比落地后报错体验好)。FolderTree 未传 onIpcWarn = root 加载失败长期静默的根源(此处绕过:入口前置校验;落地处 notify 可作后续)。修一处 recent 必 grep 所有 recent 列表(EmptyWorkspace + ExplorerHeader 两兄弟)。A133-A147 共 17 处。

## A148 — Explorer tree listDir 失败静默(FolderTree 未传 onIpcWarn) (P2,限流修)

- **文件**: `src/panels/Explorer/tree-config.ts:63`(默认 onIpcWarn=console.warn)+ `src/panels/Explorer/FolderTree.tsx`(未传 onIpcWarn)
- **问题**: A147 沉淀里标注的后续项,codex 同轮捞出。createDataLoader 的 listDir 失败(root 或任意展开子目录)调 `onIpcWarn`,FolderTree 未传 → 落回默认 `console.warn`,用户/AT 完全无感知(树静默显空)。
- **修复**: FolderTree 给 createTreeConfig 传 onIpcWarn —— `console.warn`(诊断)+ **3s 限流** notify.error(`lastIpcWarnAtRef`,对齐 A144/展开深树多次 listDir 不刷屏)。`notify.error(localizeErrorByCode(code, message))`,message 为内部含路径的描述(仅 unknown code 作 fallback 显示)。
- **测试**: rename spec 末尾新增 `a11y(A148)` 两例(经真实 `config.dataLoader.getChildrenWithData` + listDir mock 失败)—— 单次 !ok → notify 一次;连续三次失败 → 限流只一次。放文件最后(本例把 listDir 设失败,后续无用例依赖默认 ok)。
- **沉淀**: 「可选回调有默认静默实现」(onIpcWarn 默认 console.warn)是隐藏的 silent-failure —— 调用方不传就静默,grep 默认回调赋值处(`= (...) => console.warn`)能批量发现。高频路径(树展开)的失败反馈必限流(A144 同精神)。A133-A148 共 18 处,silent-failure 方向已从 fire-and-forget reject 扩到「默认静默回调」。

## A149 — 拖文件夹到窗口打开 workspace 失败静默 (P2,抽纯函数 + 三态判别)

- **文件**: `src/shell/App.tsx:74` window onDrop + `src/lib/window-drop.ts`
- **问题**: A141 同族。`pickDroppedDirectory` 内部 catch isDir 错误 → 返 false。拖**文件夹**但 listDir 失败/reject(已删/无权限)与「只拖文件(暂不处理)正常跳过」都 → null,无法区分 → 拖文件夹失败时静默无反馈,用户「拖了没反应」。
- **修复**: 抽纯函数 `resolveDroppedWorkspace(data, hadDirectory, getPath, listDir)` 返判别式三态 —— `open`(可打开)/ `error`(拖了目录但 listDir 失败/reject,带 code)/ `ignore`(只拖文件,静默)。`hadDirectory` 由 App.tsx 在事件回调里**同步** `webkitGetAsEntry` 探测(进 async 后 DataTransferItem 失效)。App.tsx 据三态 setRoot / notify.error / 静默 + 外层 try/catch 兜底。
- **测试**: window-drop spec 新增 `resolveDroppedWorkspace` 四例(open / 目录listDir失败error / 目录reject error / 只拖文件ignore)。
- **沉淀**: 「helper 内部吞错返 falsy」(pickDroppedDirectory catch→false)使调用方无法区分「正常 falsy」与「错误 falsy」→ 需把错误**上抬成判别式结果**(open/error/ignore)而非布尔。同步资源(webkitGetAsEntry/DataTransferItem)必须在事件回调同步读取,不能带进 async。TS 闭包内赋值的 `let` 在后续 if 会被 CFA 窄化成 never → 用对象 box 保留联合类型。A133-A149 共 19 处。

## A150 — FolderTree onDropForeign row 级外部文件 drop 无 catch (P2)

- **文件**: `src/panels/Explorer/FolderTree.tsx:235` `onDropForeign`(row 级外部 OS 文件 drop)
- **问题**: A141 同族。`void (async()=>)()` fire-and-forget。performDrop 经 A137 已恒返 DropResult 不 reject,但 `partitionDropItems(dataTransfer.items)` / `refreshParent` 仍可能抛 → 无 catch 则 unhandled rejection + 批量错误无 toast/live region 反馈。
- **修复**: body 包 try/catch;catch → `notify.error(localizeErrorByCode(code, msg ?? code), { code })`,code 取 `err.code ?? 'EXCEPTION'`。保留既有 skippedDirs/!r.ok 的 msgs notify。
- **测试**: rename spec 新增 `a11y(A150)` —— 经 `config.onDropForeignDragObject`(tree-config 转发到 onDropForeign),传 `items.length` getter 抛错的 Proxy → partitionDropItems 抛 → catch → notify.error 一次。
- **沉淀**: 「上游依赖已修(performDrop 不再 reject)」不等于「此入口安全」—— 同一 async body 内还有其他可抛点(partitionDropItems/refreshParent),fire-and-forget 须整体 try/catch 而非依赖单个被调函数的健壮性。A133-A150 共 20 处,silent-failure / fire-and-forget reject 族已系统覆盖 Explorer(rename/drop×3/paste/root-drop/foreign-drop/open/trash/create/reveal/recent/selectDirectory/tree-listDir)+ Terminal(create/write/readHistory/drag-drop)+ App(window-drop)。

## A151 — FolderTree handleDrop 外部文件分支(空白/root drop)无 catch (P2)

- **文件**: `src/panels/Explorer/FolderTree.tsx:658` `handleDrop` 外部 OS 文件分支(容器 onDrop)
- **问题**: A150(row 级 onDropForeign)/A136(handleDrop 内部 root-drop 分支)同族,第三个 handleDrop 出口。handleDrop 是 `async` 事件处理器(React 不 await),外部文件分支 `partitionDropItems`/`refreshParent` 抛 → unhandled rejection + 批量错误无 toast/live region 反馈。
- **修复**: 外部分支 body(partitionDropItems→performDrop→refreshParent→msgs)包 try/catch;catch → `notify.error(localizeErrorByCode(code, msg ?? code), { code })`。保留既有 skippedDirs/!r.ok msgs notify。
- **测试**: rename spec 新增 `a11y(A151)` —— fireEvent.drop 容器(types 含 'Files' + items.length getter 抛)→ partitionDropItems 抛 → catch → notify.error 一次。
- **沉淀**: 同一 handleDrop 有三个 drop 出口(内部 root-drop A136 / row foreign A150 / 容器外部 A151),每个独立 fire-and-forget 都须各自 try/catch —— 「一个 handler 多分支」时修一个分支不覆盖其余兄弟分支。A133-A151 共 21 处。

## A152 — Markdown 预览链接 Cmd/Ctrl+click 跳转无键盘等价 (P2,键盘可达性)

- **文件**: `src/panels/Editor/MilkdownEditor.tsx:95`(handleClick)
- **问题**: 方向从 silent-failure 转回键盘可达性。Markdown 预览里 `handleClick` 仅响应 Cmd/Ctrl+**click** → onLinkClick(跳转/打开链接,IDE 约定)。键盘用户/屏幕阅读器无法用鼠标修饰键,无任何键盘等价 → 无法激活链接跳转。
- **修复**: 新增 `handleKeyDown`(挂 wrapper div onKeyDown)—— **Cmd/Ctrl+Enter**(用修饰键而非裸 Enter,避免与可编辑 ProseMirror 换行冲突)→ 定位链接(优先 event target.closest('a'),退回当前选区 anchorNode 所在 `<a>`)→ onLinkClick(href)。镜像 handleClick 的 href 守卫 + preventDefault/stopPropagation。
- **测试**: 新建 `markdown-link-keyboard/link-keyboard-activate.spec.tsx` 四例 —— Cmd+Enter / Ctrl+Enter on `<a>` → onLinkClick(href);裸 Enter → 不触发(留给换行);Cmd+Enter 不在链接上 → 不触发。复用 milkdown-preview mock harness。
- **沉淀**: 鼠标修饰键交互(Cmd/Ctrl+click)必须有键盘等价,键盘等价选「修饰键 + Enter」而非裸 Enter(裸 Enter 在可编辑区有语义冲突)。contenteditable 里链接非 Tab 可聚焦 → 键盘定位须兼顾 event target 与 selection anchorNode 两条路径。A133-A152 共 22 处(silent-failure 21 + 键盘可达 1)。

## ✅ 收敛(2026-06-25)

codex 第 153 轮(auto-compact 后)穷尽复核后输出 `• 未发现问题` + `###CODEX-DONE###`。复核覆盖面(codex 自述):菜单键盘模型(trigger/menuitem roving/Escape·Tab 关闭/前向聚焦)、链接(icon-only 无名形态已补 aria-label)、表单/裸 async 残留、dock tab/remove(已有独立处理)、Spinner `Loading` status 泛化、aria-hidden 包裹交互元素 —— 均无新可操作 a11y 缺口(剩余为后台同步/缓存类 warning,非用户可直接操作面)。

**本 session(a11y 方向)统计**:A60–A152 全部修复;最后一段 A133–A152 共 22 处(silent-failure / fire-and-forget reject 21 + 键盘可达 1);收敛时 test 全绿 3641 passed(+2 skipped),typecheck(4 tsconfig)+ lint 全绿。未 commit(user 自管,与 topic-54/55/56 数据安全/跨平台/i18n 同分支 feat/cross-platform-p0 累积)。

**A133–A152 族谱**:silent-failure「`await fs.X()` 只判 r.ok、漏 reject / fire-and-forget 无 catch」系统覆盖 —— Explorer(rename/onDropItems/paste/root-drop/foreign-drop row+容器/open/trash/createFile+Dir/reveal/recent-root/selectDirectory/tree-listDir onIpcWarn)+ Terminal(create/write 限流/readHistory/drag-drop)+ App(window-drop 三态判别)+ Editor(链接键盘等价)。两条防刷屏限流(A144 按键 write / A148 树 listDir)。一处「默认静默回调」(onIpcWarn 默认 console.warn)。一处「helper 内部吞错返 falsy」上抬为判别式三态(A149)。
